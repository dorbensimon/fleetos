# סיכום מיקה לצוות — 2026-08-24 (סבב שלישי — נתונים חיים סוף סוף)

**בקצרה: מ-2026-08-24 יש `SUPABASE_SERVICE_ROLE_KEY` ב-`.env.local`, ו-`supabase-cli.js` עוקף RLS בהצלחה. הרצתי את ארבע הבדיקות הכמותיות שהיו פתויות בשני הסבבים הקודמים — מול דאטה אמיתי, לא ניחוש.** ה-DB בפועל זעיר מאוד כרגע (חברה אחת פעילה, `vehicles: 1`, `profiles: 5`, `driver_details: 1`, `documents: 4`, `compliance_items: 3`, `departments: 1`, `notifications: 2`) — כנראה סביבת פיתוח/סטייג'ינג, לא פרודקשן בהיקף אמיתי. חשוב: היקף כזה אומר ש"0 נמצא" בשלוש מתוך ארבע הבדיקות הוא לרוב **טריוויאלי** (N=1 בטבלאות הרלוונטיות) ולא הוכחה שהבעיה המבנית נפתרה.

## תוצאות ארבע הבדיקות

**1. רשומות יתומות (`compliance_items`/`documents` עם `owner_type='driver'` ו-`owner_id` שלא קיים ב-`profiles`):** 0 יתומים נמצאו. שלוש רשומות ה-`documents` היחידות עם `owner_type='driver'` מצביעות על פרופיל קיים. **לא מפריך** את הממצא המבני (עדיין אין FK/cascade אמיתי) — פשוט עוד לא נמחק אף נהג עם דאטה תלוי בסביבה הזו. הסיכון עדיין פתוח.

**2. שיוך כפול נהג-רכב (`primary_driver_id`):** יש רכב יחיד בטבלה — מתמטית אי אפשר לבדוק כפילות עם N=1. **הבדיקה לא באמת רלוונטית עדיין.** הממצא המבני (חסר unique constraint) נשאר פתוח, ועדיין ממתין לעיצוב מחדש (`vehicle_drivers`) לפי ההחלטה המוצרית של "ריבוי נהגים לרכב" שכבר תועדה בסבב הקודם.

**3. כפילויות `national_id`/`license_number`:** יש רשומת `driver_details` יחידה — שוב N=1, טריוויאלי. הממצא המבני נשאר פתוח.

**4. סטטוס הרצה של מיגרציות 29-31 — הממצא המשמעותי ביותר בסבב הזה:**
`pg_policies`/`information_schema.routines` לא נגישים דרך PostgREST (מגבלת כלי, לא הרשאות — service_role עוקף RLS אבל לא חושף קטלוגים). **אבל יש הוכחה ישירה וחד-משמעית שמיגרציה 31 רצה בהצלחה בפועל:** העמודות `compliance_items.expiry_notified_at`/`vehicles.service_notified_at` (שנוצרות רק במיגרציה 31) קיימות, ויש שורת `notifications` אוטומטית (ללא `actor_id`) שנוצרה **באותה מיקרו-שנייה בדיוק** שבה `expiry_notified_at` עודכן על שורת ה-compliance המתאימה (`2026-08-23T17:35:29.880053+00:00`) — תואם קוד-לקוד את `check_vehicle_expiry_notifications()`. **מיגרציה 31 אושרה כרצה, בהוכחה, לא בהנחה.**

מיגרציות 29 (delete policy על notifications) ו-30 (owner check בטריגר) — לא ניתן לאשר/להפריך ישירות מהדאטה (כל השורות ב-`notifications` חדשות מדי לבדוק ניקוי-7-ימים, ואין נתונים מבחינים בין הגרסה הישנה/חדשה של הטריגר). **הערכה סבירה (לא הוכחה):** כיוון שאין migration tracking בפרויקט ומישהו מריץ קבצים ידנית ברצף, וש-31 (המאוחרת מביניהן) אושרה כרצה — סביר מאוד ש-29/30 רצו גם באותה ישיבה. ממליצה לדור/רועי לאשר סופית ב-SQL editor:
```sql
select polname from pg_policies where tablename = 'notifications';
```

**עדכון לזיכרון הפרויקטלי:** הזיכרון הקיים אומר "3 מיגרציות (29,30,31) נדחפו אך טרם הורצו" — זה כנראה **לא מדויק יותר**, לפחות לגבי 31. מומלץ לדור לעדכן את הזיכרון לאחר אישור סופי של 29/30.

## מה לא השתנה
הממצאים המבניים המקוריים (חוסר FK פולימורפי, חוסר unique constraints, FK בלי אינדקס) עדיין בתוקף — אף אחד מהם לא היה תלוי בגישה לדאטה חי, הם נובעים מקריאת הסכימה עצמה ב-`supabase/sql/`. הבדיקות הכמותיות רק אישרו/הפריכו אם הבעיה **כבר התממשה בדאטה הקיים** — והתשובה היא שברוב המקרים היא עדיין לא התממשה, כי ה-DB קטן מדי כרגע כדי לבחון את זה באמת.

לא בוצע שום שינוי במסד הנתונים או בקבצי SQL.
