# סטטוס פרויקט FleetOS — רפאל (PM/Delivery)

**עדכון אחרון:** 2026-08-24 (ערב) — MCP רשמי של Supabase חובר, כל מיגרציות 29-35 אומתו כרצות, ונסגרה פרצת אבטחה חדשה שהתגלתה בבדיקה (מיגרציה 36)

## סיכום מצב כללי
הפרויקט פעיל ובקצב טוב. **התפתחות משמעותית היום:** חובר MCP רשמי של Supabase (`.mcp.json`) שנותן לכל סוכן גישת קריאה ישירה ל-DB (advisors/migrations/tables/execute_sql read-only) — סוגר סופית את אי-הוודאות סביב מיגרציות 29-35 (כולן אומתו כרצות מול production) ואיפשר לגלות ולתקן פרצת אבטחה חדשה וקריטית שלא הייתה ידועה קודם (פונקציית cron חשופה ל-anon/authenticated, עקיפת RLS חוצת-דיירים) — נסגרה עוד באותו יום דרך מיגרציה 36 שרועי כתב וניתח. שינויים בפועל ל-DB עדיין דורשים אישור מפורש של דור בכל פעם (חסום ברמת classifier). נותר refactor לא-מקומיט של מיזוג `DriversScreen`+`VehiclesScreen` ל-`FleetScreen`.

## מבט-על לפי תחום

### מוצר (עידן)
`idan-backlog.md` מכיל 10 פריטים פתוחים, ללא סטטוס "בעבודה" מסומן. עדיפות גבוהה: Push notifications (חסום ע"י מיגרציות 29-31), החלטת מודל נהג↔רכב (P0 — חוסם תיקון DB), מעקב עלויות, מודול תאונות. אין דדליין/ספרינט מוגדר מהמשתמש.

### עיצוב (לירון)
7 ממצאים פתוחים, כולם מ-2026-08-24, ללא אף אחד "טופל" עדיין. הכי חמור: אין `accessibilityLabel`/`accessibilityRole` בכל האפליקציה (חומרה גבוהה), ואי-עקביות פלטת צבעים בין LoginScreen/FleetScreen/theme.ts.

### Frontend (מתן)
אין קובץ סטטוס/ממצאים ייעודי מ-מתן עדיין. עבודה בפועל ניכרת ב-git log (FleetScreen, VehicleDetailScreen, DateField, DriversVehiclesToggle) — כרגע **לא מקומיטת** (working tree עם שינויים לא-staged בקבצים אלו + FleetScreen.tsx untracked + מחיקת VehiclesScreen/DriversScreen). מומלץ לסגור/לקמיט לפני שממשיכים לעומק, כדי לא לאבד עבודה.

### Backend (רועי)
אין קובץ סטטוס ייעודי. שכבת ה-API (`lib/adminApi.ts`) מוזכרת בממצאי עומר/מיקה כמקום הבעיה (`updateVehicle` לא מנקה שיוך נהג-רכב ישן).

### QA / Fix (עומר / שרגה)
עומר סרק 2026-08-23/24: 2 ממצאים סומנו "טופל" (תלויים בהרצת מיגרציות 29/30 שטרם רצו — ראה חסם למטה), ו-1 ממצא **פתוח בחומרה גבוהה**: שיוך כפול נהג↔רכב (`VehicleFormScreen`/`primary_driver_id` בלי unique). אין תיעוד עבודת שרגה עדיין מול הממצא הפתוח.

### תשתית (דניאל)
אין קובץ סטטוס/ממצאים מ-דניאל עדיין — פער מידע. יש סוכן מוגדר (`.claude/agents/daniel.md`) אך ללא פלט עבודה מתועד. **חשוב:** יש עכשיו MCP רשמי של Supabase מחובר (`.mcp.json`) — דניאל (ומיקה) צריכים לדעת שיש להם גישת קריאה ישירה ל-DB (advisors/migrations/tables/execute_sql) מעכשיו, בלי לחכות לאישור על כל בדיקת read-only.

### אבטחה — עדכון 2026-08-24 (רועי + MCP)
כל 7 המיגרציות הממתינות (29-35) אומתו כרצות בפועל מול production. בנוסף התגלתה ותוקנה פרצת אבטחה **חדשה** ולא-ידועה קודם: `check_vehicle_expiry_notifications()` הייתה `SECURITY DEFINER` חשופה ל-anon/authenticated — כל משתמש מחובר יכול היה לקרוא לה ישירות ולגרום לתופעות לוואי חוצות-דיירים. מיגרציה 36 (רועי) תיקנה זאת ואומתה מול production. פירוט מלא ב-`rafael-risks.md`. פריט פתוח יחיד שנותר: `auth_leaked_password_protection` כבוי (הגדרת Dashboard, לא migration) — סיכון בינוני, לא חוסם, מומלץ כ-quick win בתחילת הסבב הבא.

### נתונים (מיקה)
סרקה 2026-08-24 (סכימה בלבד — אין הרשאת authenticated, כמדווח מפורשות בקובץ). 6 ממצאים פתוחים, כולם ללא היקף כמותי מאומת (רק ניתוח סכימה). הכי חמורים: (1) אין FK cascade לניקוי `compliance_items`/`documents` במחיקת נהג — יתומים בפועל; (2) `vehicles.primary_driver_id` בלי unique constraint — **תואם בדיוק את ממצא עומר**; (3) אישור מחדש שמיגרציות 29-31 טרם רצו.

## עדכון 2026-08-24 — טיוטת הקצאת משימות (במקום תפקיד שרגה)
כל סבבי הביקורת (עומר/לירון/מיקה/דניאל/רועי/מתן) הושלמו. בניתי טיוטת שיבוץ מלאה — מי (רועי/מתן/דניאל) עושה מה, באיזה סדר, עם תלויות — ב-`.claude/rafael-task-assignment-draft.md`. עידן ירוץ אחריי לאשר/לעדכן סדר עדיפויות מוצרי. שני חסמים דורשים החלטת דור **לפני** שממשיכים (ראו סעיף 0 בטיוטה): (א) אישור פורמלי למודל `vehicle_drivers` (ריבוי נהגים לרכב) — כרגע רק "נמסר לידיעה", לא אושר במפורש; (ב) אישור לכתיבת מיגרציה 32 לסגירת פרצת RLS מאושרת-חיה ב-`driver_details.company_id` (רועי ממתין לאישור מפורש, אין סיבה טכנית לעיכוב).

## חסמים פתוחים (Blockers)

1. ~~**מיגרציות 29/30/31 לא רצו מול ה-DB החי**~~ — **נסגר 2026-08-24.** אומת דרך MCP: כל 7 המיגרציות (29-35) רצות בפועל. תשתית ה-push notification (backlog עידן #1) כבר לא חסומה מבחינת DB.
2. **החלטת מודל עסקי "רכב↔נהג" לא התקבלה** — עידן דגל שזו החלטת מוצר (קשר יחיד מול שיוך מרובה/רכבי מאגר) שצריכה להתקבל **לפני** שמריצים unique constraint שמיקה ועומר הציעו, כי הפתרון הטכני משתנה בהתאם. חוסם גם את תיקון עומר (Bug #3 בקובץ שלו) וגם את המלצת DB של מיקה. **תלוי בהחלטת דור/עידן.**
3. **עבודת Frontend לא מקומיטת** — שינויים משמעותיים ב-FleetScreen/VehicleDetailScreen/DateField וכו' יושבים ב-working tree בלי commit. סיכון לאיבוד עבודה או קונפליקט אם ממשיכים לפתח על גביו בלי לעצור נקודת ביקורת.

## תלויות בין-צוותיות

- Push Notifications (עידן #1) ← ~~תלוי במיגרציות 29-31~~ **הוסר** — כל המיגרציות אומתו כרצות (2026-08-24). ניתן לתעדף מחדש מול עידן.
- תיקון "שיוך כפול נהג-רכב" (עומר/שרגה, DB fix של מיקה) ← תלוי בהחלטת מודל מוצר (עידן/דור) לפני מימוש.
- מודול "הוצאות רכב" (עידן) ← מומלץ לתעדף אחרי סגירת מיגרציות 29-31 ואחרי החלטת מודל נהג-רכב, כדי לא לפתוח כמה חזיתות מיגרציה במקביל (המלצת עידן עצמו בבקלוג).
- איחוד פלטת צבעי סטטוס (לירון + עידן #9 בבקלוג) ← מומלץ לסגור לפני הוספת עוד מסכי סטטוס חדשים (הוצאות/תאונות).
- נגישות (`accessibilityLabel`) — ממצא לירון, עידן ממסגר כמדיניות מוצר קבועה — ביצוע בפועל ממתין לשרגה.
