# סיכום עומר לצוות — סבב ביקורת 2026-08-24

סריקת קוד נוספת (ללא גישה חיה ל-DB, כמתואם). נוספו 2 ממצאים חדשים ל-`.claude/omer-findings.md`, שניהם בסטטוס פתוח:

1. **[גבוהה] `DriverPersonalDetailsScreen` — שיוך רכב שכבר תפוס לנהג אחר, בלי אזהרה.** זהו המסך שהקוד עצמו מתעד כ"הדרך הנכונה" לשנות שיוך נהג↔רכב, אבל גם בו אין בדיקה אם הרכב הנבחר כבר משויך לנהג אחר — התוצאה: דריסה שקטה, הנהג השני מאבד את הרכב שלו בלי התראה. זה משלים (מכיוון אחר) את הממצא הקודם על `VehicleFormScreen` מהסבב הקודם. **קשור למתן (Frontend)** אם יתקנו את שני המסכים יחד, ול**רועי (Backend)** — הפתרון האמיתי הוא `unique` constraint חלקי על `vehicles.primary_driver_id` ברמת ה-DB, לא רק תיקון UI.
2. **[נמוכה] תזמון ה-cron של מיגרציה 31 (התראות תוקף/טיפול) לא עושה מה שההערה בקוד מבטיחה** — `0 4 * * *` UTC = 07:00 IDT בקיץ, לא לפני 05:40/06:00 IDT כפי שנטען. לא נבדק מול DB חי (כמתואם), רק סקירת קוד סטטית. **קשור לדניאל (DevOps)** — תזמון cron/timezone הוא בעיקר בתחומו.

בנוסף: עדכנתי את `.claude/qa/test-plan.md` עם תרחישי בדיקה למסך `FleetScreen` החדש (מיזוג נהגים/רכבים), `DateField` (Modal חדש ל-iOS), התראות אדמין ומיגרציה 31, ותרחישי אבטחה/עומס מתועדים לבדיקה עתידית. **סטטוס תשתית: אין עדיין Jest/RTL בפרויקט** — לא הותקן ולא הורץ שום טסט אוטומטי בסבב הזה; זה פריט פתוח לכל מי שמנהל את סדר העדיפויות (רפאל?).

לא נמצאו ממצאים נוספים בתחומי VehicleFormScreen/adminApi/documents.ts/validation.ts מעבר למה שכבר מתועד.
