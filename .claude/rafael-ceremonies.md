# יומן טקסי Agile/Scrum — FleetOS (רפאל)

## Standup — 2026-08-24 (ערב)
- **משתתפים (בפועל/מדומים):** רועי (Backend), דור (בעל עניין/מריץ מיגרציות בפועל)
- **סטטוס שהוצג:**
  - רועי: חובר MCP רשמי של Supabase (`.mcp.json`) עם גישת SQL ישירה, כדי לאמת מול production אם מיגרציות 29-35 רצו בפועל (לא היה ניתן לאמת קודם דרך PostgREST בלבד). אימות: כל 7 המיגרציות רצות. בבדיקת security advisors התגלתה פרצה חדשה — `check_vehicle_expiry_notifications()` חשופה ל-anon/authenticated (עקיפת RLS חוצת-דיירים). רועי כתב וניתח מיגרציה 36, הריץ מול production (באישור דור), ואימת שכל אזהרות ה-advisor הקריטיות נסגרו.
  - דור: אישר את הרצת מיגרציה 36 מול production.
- **החלטות/action items:**
  - רפאל: לעדכן `rafael-status.md` ו-`rafael-risks.md` — לסגור את סיכון "מיגרציות לא ידוע אם רצו" ואת סיכון "cron function חשופה", ולפתוח סיכון חדש בינוני עבור `auth_leaked_password_protection` (Auth setting כבוי, לא migration).
  - דור: להחליט אם לתעדף הפעלת `auth_leaked_password_protection` עכשיו או בסבב הבא. **החלטת רפאל:** מומלץ כ-quick win בתחילת הסבב הבא — לא חוסם עבודה נוכחית, לא עקיפת בידוד דיירים כמו הסיכונים שנסגרו היום.
  - כל הסוכנים (בעיקר דניאל ומיקה): מודעות ל-MCP החדש — יש גישת קריאה ישירה ל-DB מעכשיו (advisors/migrations/tables/execute_sql), בלי לחכות לאישור על כל בדיקת read-only. שינויים בפועל עדיין דורשים אישור מפורש של דור בכל פעם.
- **חסמים שעלו:** אין חסמים חדשים. שני חסמים/סיכונים נסגרו (ראו `rafael-risks.md`). חסם ישן (החלטת מודל רכב↔נהג) נותר פתוח — לא נגע לדיון הזה.
