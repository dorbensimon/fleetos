# יומן טקסי Agile/Scrum — FleetOS

## Sprint Planning — 2026-08-25 — פיצ'ר "מסמכים" (שלב 1)
- **משתתפים (בפועל/מדומים):** רפאל (מתכלל), בהתבסס על דרישה ישירה של דור. עידן/מיקה/רועי/מתן/לירון טרם הופעלו בפועל — זהו תכנון מוקדם שמייצר להם briefing.
- **סטטוס שהוצג:** בקשה חדשה מדור — "מסמכים" בתפריט אדמין: יצירת מסמך (שם+קובץ), רשימת מסמכים, שליחה לנהג בודד או לכולם, קבלה אצל הנהג ב"מסמכים" עם אותו שם, התראה לנהג "מסמך X נשלח אליך לצורך חתימה". דור ציין מפורש שזה שלב ראשון, בלי מימוש חתימה בפועל (pad), וש"אחרי זה נתקדם".
- **בדיקת מוכנות טכנית (build-readiness) שביצע רפאל:**
  - טבלת `documents` הקיימת (bucket פרטי, owner_type driver/vehicle) **לא מתאימה כמות שהיא** למודל תבנית+שליחות מרובות — נדרשת החלטת סכימה.
  - טבלת `notifications` הקיימת **אין לה מודל נמען יחיד** (רק company-wide RLS ל-admin) — נדרש שינוי סכימה אמיתי כדי שנהג יראה התראה שנשלחה אליו ורק אליו. זה החסם הטכני המרכזי.
  - יש כבר תשתית UI קיימת שאפשר לעשות reuse: `Select` (בחירת נהג), `EmptyState`, מסך `MenuScreen.tsx` (הוספת item "מסמכים" קלה), `DriverNotificationsScreen.tsx` כבר קיים וריק עם הערה בקוד שמצפה בדיוק לפיצ'ר הזה ("that lands with the document-signature feature").
- **סדר עבודה מוצע (Task Breakdown + תלויות):**
  1. **עידן** — PRD קצר לפיצ'ר "מסמכים — שלב 1", כולל: הבהרת גבולות (בלי חתימה), מענה לשאלות הפתוחות (ראו `.claude/rafael-risks.md`: גודל/סוג קובץ, מחיקה/עריכה אחרי שליחה, נהג לא-פעיל, "שליחה לכולם" האם כולל נהגים עתידיים). **לא מתחיל אף אחד אחר עד שזה קיים**, כי בלעדיו יש סיכון scope-creep/הנחות סותרות.
  2. **מיקה + רועי (מקביל, אחרי PRD)** — תכנון סכימה: טבלאות חדשות למסמכי-תבנית ולשליחות, פתרון לנמען יחיד ב-`notifications` (או טבלה/מנגנון מקביל), RLS מלא (נהג רואה רק מסמכים שנשלחו אליו; אדמין רואה הכל בחברה שלו), מדיניות Storage לbucket הרלוונטי.
  3. **רועי** — מימוש מיגרציה + פונקציות/API (יצירת מסמך, שליחה לנהג/לכולם, יצירת התראה מקושרת).
  4. **מתן** — UI: מסך "מסמכים" באדמין (רשימה + הוספה), מסך מסמך בודד עם "שליחת מסמך" (בחירת נהג / שליחה לכולם), הוספת item לתפריט (`MenuScreen.tsx`, גם ADMIN_ITEMS וגם DRIVER_ITEMS), אזור "מסמכים" אצל הנהג, חיבור להתראה הקיימת (`DriverNotificationsScreen.tsx` כבר יש שלד).
  5. **לירון** — standby: נכנסת רק אם מתן מזהה שאין דפוס UI מתאים ל-reuse (סביר שרוב הרכיבים כבר קיימים — Select, list rows, VehicleDriversEditor כהשראה לבחירת נהג).
  6. **עומר** — QA אחרי שרועי+מתן מסיימים implementation ראשונית: בדיקת RLS (נהג לא רואה מסמכים של נהג אחר), בדיקת "שליחה לכולם" בקנה מידה, בדיקת שם המסמך זהה בשני הצדדים.
- **החלטות/action items:**
  - עידן: לכתוב PRD — **בעלים: עידן, אין דדליין קשיח מהמשתמש, ממליץ להתחיל מיידית כי הוא חוסם את כולם.**
  - רפאל: לעדכן `rafael-status.md`/`rafael-risks.md` בכל התקדמות/חסם חדש שמדווח.
  - דור: נדרש לאשר/להבהיר את השאלות הפתוחות ב-PRD של עידן לפני שמיקה/רועי נועלים סכימה סופית.
- **חסמים שעלו:** ראו `.claude/rafael-status.md` סעיף "חסמים פתוחים" — (1) אין PRD, (2) `notifications` בלי נמען יחיד, (3) `documents` לא תואמת מודל תבנית+שליחות.

---

## Sprint Planning — 2026-08-25 — פיצ'ר "מסמכים חתומים" שלב 2 (חתימה דיגיטלית בפועל)
- **משתתפים (בפועל/מדומים):** רפאל (מתכלל), בהתבסס על PRD מאושר של עידן (`.claude/prds/documents-broadcast-stage2-signing.md`), המבוסס בעצמו על החלטות ישירות של דור (25.8.2026). מיקה/רועי/לירון/מתן/עומר טרם הופעלו בפועל בסשן הזה.
- **סטטוס שהוצג:** שלב 1 ("מסמכים חתומים" — הפצה ללא חתימה) מומש במלואו בקוד (`39ed8bd`, מיגרציה 37, `lib/documentsBroadcastApi.ts`, מסכי אדמין/נהג) — אך טרם אושר שהמיגרציה רצה בפועל מול Supabase production. דור אישר עכשיו PRD שלב 2: חתימה בפועל (signature pad), מיקום חתימה מוגדר על התבנית ע"י האדמין, הפקת "מסמך מורכב" (טופס+חתימה מוטבעת, לא קובץ חתימה נפרד), stack-collapse בין עותקים ממתינים של אותה תבנית, מעבר תצוגת הסטטוס אצל האדמין למסך `DriverDetailScreen` (לא מסך התבניות), והורדת המסמך החתום למכשיר האדמין. אין שאלות פתוחות ב-PRD (סעיף 7).
- **סדר עבודה מוצע (Task Breakdown + תלויות) — פרטים מלאים ב-`rafael-timeline.md`:**
  1. **מיקה** — הצעת סכימה: עמודות מיקום חתימה על `document_templates` (FR1), RPC/SECURITY DEFINER מבוקר לחתימה על `document_sends` (בעלות + סטטוס pending בלבד, FR3), לוגיקת stack-collapse אטומית בטרנזקציה אחת (FR4), הבהרת סמנטיקה חדשה של `signature_file_path` (הנחה #5). **לא מריצה בעצמה מול DB** — migration proposal בדפוס הקיים (37 נעשה כך: מיקה מציעה, רועי סוקר/כותב/מריץ).
  2. **רועי** — סוקר את הצעת מיקה, כותב מיגרציה 38 בפועל, מממש RPC, מחליט על פורמט הקובץ המורכב (PDF עם שכבת חתימה מול rasterized image) ואיפה מתבצעת ההפקה (קליינט/שרת/edge function).
  3. **לירון** — מתחילה עיצוב **במקביל לרועי**, ברגע שיש הצעת סכימה ממיקה (לא חייבת לחכות למיגרציה שרועי מריץ): signature pad, מנגנון סימון מיקום חתימה ביצירת תבנית, תצוגת מסמך חתום+הורדה ב-`DriverDetailScreen`.
  4. **מתן** — תלוי **גם** ברועי (API/RPC) **וגם** בלירון (עיצוב) — מתחיל מימוש אמיתי רק אחרי ששניהם מסרו תוצר ראשוני. קנבס חתימה, סימון מיקום ביצירת תבנית, אינטגרציה לנתיב הכתיבה, הרחבת מסכי נהג, רכיב חדש ב-`DriverDetailScreen`, כפתור הורדה.
  5. **עומר** — QA אחרי implementation, דגש על collapse (FR4) ובעלות חוצה-נהגים (FR3) — ר' PRD סעיף 8.
- **פעולה שביצע רפאל בפועל בסשן זה:** ניסיון להפעיל את מיקה כ-subagent (`subagent_type: "mika"`) על המשימה הראשונה — **נכשל בפועל**: אין לרפאל כלי Agent/Task זמין בסשן הנוכחי (לא מופיע ברשימת הכלים). לכן המשימה למיקה מוגדרת ומתועדת כאן ובריף מלא, אך **לא הופעלה בפועל**. ר' `rafael-risks.md` — "אין לרפאל כלי הרצת סוכנים".
- **בריף מלא למיקה (מוכן להעתקה/הפעלה ישירה ע"י דור):**
  > קראי את `.claude/prds/documents-broadcast-stage2-signing.md` במלואו, בדגש על FR1, FR3, FR4, הנחה #5, וסעיף 8 ("הערה למי שממשיך — רועי/מיקה"). קראי גם את `supabase/sql/37_documents_broadcast_schema.sql` (הסכימה הקיימת של שלב 1 שאת בונה מעליה). הכיני migration proposal (כמו התבנית הקיימת ב-`.claude/mika-proposals/37_documents_broadcast_schema.sql`) הכוללת: (1) עמודות/מנגנון חדש על `document_templates` למיקום חתימה — שדה חובה, קבוע לאחר יצירה, לפחות מספר עמוד + קואורדינטות/אזור; (2) RPC יחיד (SECURITY DEFINER) שמאפשר לנהג לעדכן `status`/`signed_at`/`signature_file_path` רק על שורת `document_sends` שבה `driver_id = auth.uid()` וגם `status = 'pending'`, ושמבצע גם את ה-collapse (כל שאר השורות `pending` באותו `(driver_id, template_id)`) באותה טרנזקציה אטומית; (3) `signed_at` הוא זמן שרת (`now()`), לא קלט מהקליינט. אל תריצי בעצמך מול DB — זו הצעה בלבד, רועי סוקר וכותב/מריץ את המיגרציה בפועל, בדיוק כמו בדפוס של מיגרציה 37.
- **החלטות/action items:**
  - דור: להפעיל את מיקה בפועל עם הבריף לעיל (רפאל לא יכול).
  - דור/דניאל: לאמת שמיגרציה 37 (ורצוי גם 29-31) אכן רצות מול Supabase production, לפני שרועי בונה מעליה מיגרציה 38.
  - רפאל: לעדכן `rafael-status.md` ברגע שמיקה מוסרת הצעת סכימה בפועל.
- **חסמים שעלו:** ר' `rafael-risks.md` — "אין לרפאל כלי הרצת סוכנים" (חדש), "מיגרציה 37 לא ידועה כרצה בפרודקשן" (חדש), "מורכבות stack-collapse אטומי" (חדש), "הרחבת scope מעבר להיקף שלב 2" (עודכן משלב 1).

---

## Sprint Planning — 2026-08-29 — RE-BASELINE: Docuseal בפועל + 3 משימות חדשות מהקורדינטור/דור

**הקשר:** תוך כדי עבודה על משימה חדשה (Admin Notification Routing) גיליתי שהתוכנית שתועדה למעלה (שלב 2 — signature pad/RPC/collapse עצמאי) **הוחלפה בפועל** באינטגרציית Docuseal (שירות חתימה חיצוני) שכבר מומשה ונ-commit-ה (`3b488d9`), ללא מעורבותי. ר' `rafael-status.md` סעיף "RE-BASELINE קריטי" לפירוט המלא. **התוכנית למטה מבוססת על המצב האמיתי בקוד, לא על ה-PRD הישן.**

**מגבלה חשובה שחוזרת:** אין לי (רפאל) כלי Agent/Task להפעלת סוכנים אחרים בסשן זה. הבריפים למטה **מוכנים להעברה/הפעלה ישירה על ידי דור** לכל סוכן — אני לא יכול "ללחוץ כפתור" ולגרום להם לרוץ בפועל. לא אבצע קוד/מיגרציה/commit בעצמי בשום מקרה.

### עבודה 1 — 3 באגי P0 למתן (אותו איזור קוד, לטפל ביחד)

**בעלים:** מתן. **חוסם:** שלב 1 של Docuseal בפועל.

| # | באג | ממצא רפאל (קריאת קוד, לא תוקן) |
|---|---|---|
| 2 | תמונת תבנית לא מוצגת ב-`SignatureAreaMarker`/preview | ראה `rafael-status.md`, טריאז' קודם — ייתכן שהקובץ הזה כבר לא רלוונטי אחרי המעבר ל-Docuseal (יש `DocusealWebViewScreen.tsx` חדש); **מתן צריך לוודא קודם אם הבאג הזה עדיין קיים בזרימה הנוכחית או שהוא היה שייך לגרסה הישנה שנמחקה.** |
| 6 | תצוגת מסמך + אפשרות סימון מיקום מחדש | כנ"ל — ה-flow השתנה ל-`DocusealWebViewScreen` (mode: builder/document/sign). ייתכן זה כבר פתור בארכיטקטורה החדשה (Docuseal מנהל את סימון המיקום בעצמו ב-builder session) — **אימות ראשון, לא תיקון עיוור.** |
| 12 | לא ניתן להעלות מסמך שאינו תמונה-מהגלריה (PDF/Files app) | **חדש, זוהה 29.8.2026.** ממצאי בדיקת קוד מלאים ב-`rafael-status.md`. תמצית: (א) `startBuilderFrom('file')` משתמש ב-`pickFile()` מ-`lib/documents.ts` — פונקציה שנבנתה לפיצ'ר אחר (מסמכי נהג/רכב), עכשיו עושה reuse ל-Docuseal; (ב) חשד מוביל: `Image.getSize()` ב-`imageToPdf()` (`lib/docuseal.ts:73-81`) לא אמין מול URI-ים מסוג `content://` שמחזיר `DocumentPicker` (להבדיל מ-`ImagePicker` שדרכו "תמונה מהגלריה" עובד); (ג) לוודא מול תיעוד Expo v54 המדויק (חובה לפי AGENTS.md) אם `base64: true` נתמך ב-`DocumentPicker.getDocumentAsync`. **צעדים מוצעים למתן:** (1) שחזור — נסה להעלות PDF אמיתי מ-Files app על מכשיר פיזי; (2) בדוק אם זו רגרסיה מה-`imageToPdf`/image-dimension-detection שנוסף היום (`CLAUDE_HANDOFF_2026-08-29.md` פריט 4); (3) תיקון + טסט חדש ב-`lib/__tests__/docuseal.test.ts` (יש כבר טסטים דומים שם מהיום — image dimensions, PDF bypass, filename sanitization — להוסיף מקרה שמכסה קונקרטית את התרחיש שנכשל). |

**סדר עבודה מומלץ:** #12 קודם (root cause ברור יותר, חוסם ב-production-readiness), אח"כ #2/#6 — אבל קודם לבדוק אם #2/#6 בכלל עדיין רלוונטיים בארכיטקטורת Docuseal החדשה (יכול להיות שכבר נפתרו כלאחר-יד באינטגרציה).

### עבודה 2 — Admin Notification Routing (P1, חדש)

**בעלים:** מתן (routing+params) → רועי (payload) → עומר (QA). ר' טבלת המיפוי המלאה ב-`rafael-status.md`.

**ממצא רפאל (מאומת בקוד):** `screens/admin/NotificationsScreen.tsx`, פונקציה `targetRouteForNotification` (שורה 56-65) — קומפוננטה משותפת admin+driver, אך הענף כולו מותנה ב-`if (profile?.role !== 'driver') return null;` (שורה 59). **כלומר: אדמין שלוחץ על כל התראה, מכל סוג, לא מנווט לשום מקום — תמיד.** זה מאמת במדויק את מה שדור תיאר.

**בריף למתן:**
> תרחיב את `targetRouteForNotification` (או תפצל ללוגיקה נפרדת לאדמין) ב-`screens/admin/NotificationsScreen.tsx` כדי לתמוך ב-5 סוגי ניתוב חדשים לאדמין (טבלה מלאה ב-`rafael-status.md`): מסמך→`AdminDocumentSigningScreen`, נהג עודכן→`DriverPersonalDetailsScreen`, הקצאת רכב→`VehicleDetailScreen`, בדיקת רכב→`VehicleDetailScreen`, שינוי העדפות→`NotificationPreferencesScreen`. שים לב: הפונקציה הנוכחית מחזירה **רק שם מסך**, בלי פרמטרים — תצטרך להרחיב את ה-navigate כדי להעביר `driverId`/`vehicleId`/`documentId` בפועל (אחרת המסך ייפתח ריק/בלי הקשר). תלוי בשלב הבא (רועי) לגבי אילו IDs זמינים בפועל ב-payload של כל סוג התראה.

**בריף לרועי:**
> וודא שה-payload של כל סוג התראה שרלוונטי לאדמין (בטבלת `notifications`, לא `driver_notifications`) כולל בפועל עמודת/שדה עם ה-ID הרלוונטי (driver_id / vehicle_id / document/template id) ולא רק טקסט הודעה חופשי. אם חסר — זו תוספת עמודה קטנה + עדכון קוד היצירה של ההתראה, לא מיגרציה גדולה. בדוק את `lib/adminApi/notifications.ts` ואת המיגרציות הרלוונטיות (25, 30, 40, 42) לראות אילו IDs כבר נשמרים.

**בריף לעומר:**
> אחרי שמתן מסיים — לחיצה על כל אחד מ-5 סוגי ההתראות (טבלה ב-`rafael-status.md`) חייבת לפתוח את המסך הנכון **עם הנתונים הנכונים טעונים** (לא מסך ריק). בדוק במיוחד את "מסמך נשלח לחתימה" — עם המעבר ל-Docuseal יכול להיות שסוג ההתראה הזה בכלל לא קיים עדיין באדמין (תלוי אם מישהו יצר notification type מתאים בקוד/מיגרציה 40/42).

### עבודה 3 — Docuseal Multi-Tenancy by company_id (P2, לפני production go-live רחב)

**בעלים:** מיקה (proposal) → רועי (migration+RLS) → מתן (frontend validation) → עומר (QA isolation test).

**הקשר מעודכן:** הדרישה הזו רלוונטית **יותר** משחשבתי כשהוספתי אותה קודם כ-P2 כללי — כי עכשיו יש בפועל טבלאות Docuseal אמיתיות (`SigningTemplate` עם `company_id` כבר קיים כשדה בטיפוס ב-`lib/docuseal.ts:8-16`! צריך לוודא בפועל אם ה-RLS policies שכבר נכתבו במיגרציה 37/39 (`docuseal_signing.sql`, `legacy_schema_rls_cleanup.sql`) **כבר אוכפות** company_id isolation, או שזו רק עמודה בלי RLS תואם — זה שינוי משמעותי בהערכת הדחיפות: **ייתכן שהעבודה כבר נעשית חלקית ורק צריך אימות, לא בנייה מאפס.**

**בריף למיקה (מעודכן):**
> **לפני שמציעה שינוי סכימה חדש** — קראי את `supabase/sql/37_docuseal_signing.sql` ו-`39_legacy_schema_rls_cleanup.sql` הקיימים ובדקי אם RLS policies על טבלאות ה-signing templates/requests כבר אוכפות `company_id = (select company_id from profiles where id = auth.uid())` (או מנגנון שקול). אם כן — המשימה שלך היא **אימות בלבד** + דוח קצר. אם לא (למשל יש company_id בטיפוס אך אין לו RLS אמיתי, או שהוא nullable) — תציעי תיקון ממוקד (constraint NOT NULL + policy), לא בנייה מחדש.

**בריף לרועי (מעודכן):**
> ממתין להערכת מיקה. אם RLS כבר קיים ותקין — סגור פריט. אם לא — migration קטנה וממוקדת (לא migration 38 גדולה כמו שתוכנן בעבר לגישה הישנה).

**בריף למתן:**
> אחרי אישור רועי/מיקה על מצב ה-RLS: וודא ברמת קליינט (UX, לא security — ה-RLS הוא ה-guarantee האמיתי) שרשימת ה-templates המוצגת לאדמין מסוננת לפי company_id שלו (כנראה כבר קורה דרך `useCompany()`/`companyId` הקיים בכל הקריאות — ר' `AdminDocumentSigningScreen.tsx:31` — צריך רק לוודא, לא לבנות מנגנון סינון חדש).

### עדכון סטטוס מיגרציות — פתוח, דורש דניאל
מיגרציות 37-40 (Docuseal) לא ידוע אם רצו בפועל מול production (41 כן לפי handoff). מיגרציה 42 מוכנה מקומית ולא הוחלה — **ממתינה לאישור מפורש של דור** לפני apply. אני (רפאל) לא אריץ שום דבר בעצמי.

### Action items
- דור: להעביר את הבריפים למתן/רועי/מיקה/עומר (אני לא יכול להפעיל אותם). לאשר/לדחות apply של מיגרציה 42 מול דניאל.
- דניאל: לאמת סטטוס מיגרציות 37-40 מול production.
- כולם: לדווח לרפאל כשמתקדמים כדי שאני אעדכן `rafael-status.md`.

---

## Briefing — 2026-09-01 — DriverFormScreen לא תואם עיצוב, הכרעת טוקנים ללירון לפני מתן

**הקשר:** דור בדק בפועל את `screens/admin/DriverFormScreen.tsx` (שנכתב מחדש ע"י סוכן קוד לפי מפרט עיצוב חיצוני `מפרט מסך נהג חדש.md`) וקבע שהנראות גרועה (רקע אפור, כפתור לא נכון). בדיקת קוד (קריאה בלבד) אישרה שזו התנגשות אמיתית בין המפרט לבין `lib/theme.ts` — ר' פירוט מלא ב-`rafael-status.md` ("עדכון קריטי 01.09.2026") וב-`rafael-risks.md` ("מפרטי עיצוב חיצוניים מתנגשים עם lib/theme.ts"). דור אישר תהליך: רפאל מתאם בלבד (לא נוגע בקוד), לירון מכריעה בהתנגשות המפרט מול הטוקנים, מתן ממש מחדש. **אין לי כלי הרצת סוכנים בסשן זה — דור/הסשן המתזמר צריך להפעיל את לירון ואת מתן ישירות עם הבריפים הבאים, בסדר הזה.**

**בריף ללירון (עיצוב) — ראשון בתור, חוסם את מתן:**
> יש תקלת עיצוב מדווחת של דור על `screens/admin/DriverFormScreen.tsx` (שכתוב טרי לפי מפרט חיצוני `C:\Users\dor59\Downloads\מפרט מסך נהג חדש.md`). המשימה שלך היא **לא** לייצר מוקאפ חדש (הוא כבר קיים במפרט) — אלא לייצר **מסמך התאמה ממוקד**, באותו סגנון כמו `.claude/liron-design/design-system.md` הקיים שלך, שממפה כל דרישה מהמפרט לטוקן/קומפוננטה **קיימים בפועל** בקוד: רקע המסך, אווטאר ה-hero, צל הכרטיסים, ה-CTA הצף, מצב-נבחר בקרוסלת דרגת הרישיון, פס ההתקדמות, ומתג הזמנת ה-SMS. תצטרכי לפתור במפורש את התנגשות הרקע/גרדיאנט: המפרט מבקש רקע `#F1F4F7` וגרדיאנט `accent-grad` (`linear-gradient(180deg,#3FA9E8,#0A7FD0)`), אבל `lib/theme.ts` מצהיר במפורש `COLORS.screen = '#E4E4E4'` ו"#0088CC is the ONLY accent colour" (בלי גרדיאנטים בכלל) — את מחליטה איזה טוקן קיים משמש לכל דרישה (או אם צריך תוספת טוקן קטנה ועקבית, לא המצאת פלטה חדשה אד-הוק). תעני על כל סעיף ברמת קומפוננטה קונקרטית כך שמתן יוכל לעקוב 1:1 בלי לפרש. קבצי ייחוס: המפרט המקורי (`מפרט מסך נהג חדש.md` בנתיב לעיל), `lib/theme.ts`, `components/ui/index.tsx` (כבר מכיל `Screen`/`Card`/`ScreenHeader`/`PrimaryButton` בשימוש תקין במסכים אחרים כמו `VehicleFormScreen.tsx`/`FleetScreen.tsx`).

**בריף למתן (Frontend) — רק אחרי שמסמך ההתאמה של לירון מוכן:**
> תשכתב את `screens/admin/DriverFormScreen.tsx` לפי מסמך ההתאמה שלירון תכין (ראו בריף ללירון למעלה — אל תתחיל לפני שהוא קיים וסגור). דגשים קונקרטיים לתיקון: (1) reuse בפועל של `Screen`/`Card`/`ScreenHeader`/`PrimaryButton` הקיימים ב-`components/ui/index.tsx` במקום בנייה ידנית מחדש עם `View`/`TouchableOpacity`+`StyleSheet.create` בקובץ; (2) הסרת שלוש התחבולות `require('react-native').StyleSheet.create(...)`; (3) תיקון הבאג ב-`styles.card` — יש בו גם `shadow*` וגם `overflow:'hidden'` על אותו style object, מה שמבטל את הצל ב-iOS, לפי מה שלירון תקבע לגבי הצל הרצוי; (4) הסרת ה-`rgba(238,245,250,.95)` הקשיח שנקבע ל-header, לפי טוקן שלירון תמפה; (5) שימור מלא של הלוגיקה ההתנהגותית הקיימת — מעקב התקדמות 7 שדות חובה, ולידציות, קרוסלת דרגות רישיון (סווייפ), date picker לתוקף רישיון, מתג הזמנת SMS (ויזואלי בלבד, בלי backend), ובורר המחלקה — התלונה של דור הייתה ויזואלית בלבד, לא פונקציונלית, אל תשנה התנהגות. (6) בסיום: `npm run typecheck` נקי.

**Action items:**
- דור/הסשן המתזמר: להפעיל את לירון עם הבריף לעיל; אחרי שהיא מוסרת מסמך התאמה, להפעיל את מתן.
- רפאל: לעדכן `rafael-status.md` ברגע שלירון מוסרת מסמך התאמה ושוב כשמתן מסיים שכתוב + typecheck.
