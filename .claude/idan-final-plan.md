# תוכנית עבודה סופית ומאושרת — סבב 2026-08-24

**סטטוס:** מאושר. מבוסס על `.claude/rafael-task-assignment-draft.md` (חלוקת אחריות טכנית: רועי/מתן/דניאל) + סדר עדיפויות מוצרי של עידן + שני אישורים מפורשים של דור.

## שני האישורים שניתנו היום (לא עוד "לידיעה בלבד")

1. **מיגרציה 32 — RLS fix ב-`driver_details.company_id`.** מאושר לביצוע מיידי. חומרה גבוהה ביותר — פרצת בידוד multi-tenant מאומתת חיה (session אמיתי, לא תיאורטי) על ידי רועי.
2. **מודל שיוך רכב↔נהג — ריבוי נהגים לרכב.** מאושר סופית כהחלטת מוצר: רכב יכול לקבל כמה נהגים; טבלת קישור `vehicle_drivers`; הוספת נהג נוסף **לא** דורסת את הנהג הראשי/הקיים. פירוט מלא ב-`.claude/idan-backlog.md` (הכרטיס "מודל רכב↔נהג: שיוך מרובה", סטטוס: מאושר לביצוע).

זה מסיר את שני החסמים (החלטה A והחלטה B) שרפאל סימן בסעיף 0 של הטיוטה שלו. להלן סדר הביצוע המשולב.

---

## מסלול 1 — יכול להתחיל מיד, ללא כל תלות

אלה לא ממתינים לשום דבר — לא זה לזה, ולא לאישורים דלעיל (הם כבר עברו את הסף לפני האישורים, ונשארים כפי שהם):

### דניאל (מיידי, במקביל לכל השאר)
- 1.11 — תיקון תזמון cron מיגרציה 31 (04:00 UTC)
- 1.12 — `.gitignore` — הוספת שורת `.env` גורפת
- 1.13 — הוספת `"typecheck": "tsc --noEmit"` ל-`package.json`

שלושתם קטנים, עצמאיים, בלי תלות בשום סוכן אחר — דניאל יכול לרוץ עליהם ברצף היום.

### מתן (מיידי, במקביל)
- 1.5 — Error State גורף (`ErrorState` משותף, עטיפת `load()`/`CompanyContext` ב-try/catch בכל המסכים)
- 1.6 — `accessibilityLabel`/`accessibilityRole` — התחלה מהכפתורים השכיחים (חיוג, עריכה, מחיקה, ניווט חזרה, פעמון)

**סדר פנימי מומלץ למתן:** 1.5 לפני 1.6 — Error State הוא תשתית משותפת (קומפוננטה חדשה ב-`components/ui`) שכדאי שתהיה קיימת לפני שמתחילים לגעת בעוד ועוד מסכים לצורך נגישות, כדי לא לגעת פעמיים באותם מסכים בפערי זמן קצרים.

### רועי — תנאי סף בלבד
לפני שרועי נוגע בכל DB/מיגרציה כלשהי: **קומיט/סנכרון עבודת Frontend הקיימת** (סעיף 3.4 בטיוטת רפאל — working tree לא מקומיט כרגע, ראו git status). זה לא "משימה" של רועי אלא תנאי סף תפעולי לכל הצוות — מתבצע ע"י מתן/דור, לא ע"י רועי, אבל **חייב לקרות לפני שלב 2 למטה**.

---

## מסלול 2 — רועי, לפי סדר, עכשיו שיש אישור

**חשוב:** שני האישורים ניתנו יחד, אבל **מיגרציה 32 קודמת** לעבודת `vehicle_drivers` — לא כי "רק ככה", אלא כי מדובר בפרצת אבטחה חיה מול פיצ'ר מוצרי, וגם כי משימה 2.1 (תכנון `vehicle_drivers`) עדיפה שתתבצע במיגרציה נפרדת ומסודרת ולא "תיתקע" מאחורי תיקון אבטחה דחוף.

### שלב 2א (ראשון, דחוף) — מיגרציה 32 ונלוות
כל הפריטים הבאים באותה מיגרציה/ישיבת עבודה אחת (רועי כבר המליץ על כך בטיוטה, ועידן מאשר את הצירוף):
1. **1.1 — RLS fix ב-`driver_details.company_id`** (הפריט המאושר, עדיפות עליונה בכל התוכנית).
2. 1.2 — `documents` self-service policy: הוספת `company_id` guard.
3. 1.3 — טריגר ניקוי רשומות יתומות במחיקת נהג/אדמין.
4. 1.4 — בדיקת שגיאת `profiles.delete()` לפני `auth.admin.deleteUser`.
5. 3.3 — **באותה ישיבה**, אימות שמיגרציות 29/30 אכן רצו בפועל (`select polname from pg_policies where tablename = 'notifications';`) — כדי לא לפתוח עוד סבב מיגרציות נפרד לבדיקה בלבד.

### שלב 2ב (מיד אחרי 2א) — תכנון וביצוע `vehicle_drivers`
6. **2.1 — עיצוב וביצוע סכימת `vehicle_drivers`** לפי המודל המאושר: `vehicle_drivers(vehicle_id, driver_id, is_primary, assigned_at, unassigned_at?)`. רועי סוגר בפועל את שתי השאלות הפתוחות שנותרו מטיוטת רפאל:
   - האם `is_primary` יחיד לרכב (partial unique index על `vehicle_id where is_primary`) — כן, מומלץ, כדי לשמר משמעות ל"נהג ראשי" גם במודל מרובה-נהגים.
   - האם עם היסטוריה (שדה `unassigned_at`) — כן, מומלץ, כי `idan-backlog.md` כבר מזהה צורך עתידי ב-audit log על שיוכים, ועדיף לא לבנות פעמיים.
   - מדיניות RLS: נהג רואה רק שורות שבהן `driver_id = auth.uid()`; אדמין רואה/עורך הכל בתוך `company_id` שלו (דרך `vehicles.company_id`).

   **עדכון 2026-08-24 (PRD סגור):** נוספו שני פרטי מוצר משלימים שרועי צריך לקחת בחשבון בעיצוב הסכימה: **מגבלה של עד 2 נהגים לרכב** (ראשי + משני, לאכוף באידיאלי גם ב-DB דרך check/טריגר, לא רק ב-UI), ו**נהג ראשי אינו חובה** — רכב יכול להישאר זמנית ללא שורה עם `is_primary=true`. פירוט מלא ב-`.claude/prds/vehicle-drivers-multi.md` (סעיף "החלטות מוצר").

**תלות:** שלב 2ב תלוי בסיום 2א (לא טכנית חובה, אלא כדי שרועי לא "יקפוץ" בין הקשר מיגרציית-אבטחה דחופה לתכנון סכימה חדש; וגם כדי שהמיגרציה שכוללת את פרצת ה-RLS לא "תיתקע" בתור אחרי עבודה גדולה יותר).

---

## מסלול 3 — מתן, המשך אחרי 1.5/1.6 ואחרי שרועי סיים 2ב

**זו התלות המרכזית בתוכנית: מתן לא יכול לבנות UI סביב `vehicle_drivers` לפני שהטבלה קיימת בפועל.**

7. **2.2 — `VehicleFormScreen`:** UI לבחירת/הוספת כמה נהגים לרכב, בלי לדרוס שיוך קיים (ה"לא דורס" הוא עכשיו requirement מאושר, לא הצעה).
8. **2.3 — `DriverPersonalDetailsScreen`:** מניעת שיוך כפול לא-מכוון מהכיוון ההפוך (הוספת רכב לנהג).
9. **2.4 — `DriverVehicleScreen`/`DriverHomeScreen`:** מעבר מ-`.maybeSingle()` (הנחת רכב יחיד) ל"רשימת רכבים" — נדרש כעת באופן ודאי (לא "אם ההחלטה תהיה X" כפי שרפאל ניסח בטיוטה — ההחלטה כבר ריבוי-נהגים, אז זה שינוי UX מחייב, לא תיקון רשת-ביטחון קטן).

**המלצה לסדר פנימי:** 2.2+2.3 ביחד (שני צדדים של אותה בעיית שורש), ואז 2.4 (תלוי גם בהם מבחינת עקביות המודל שמוצג לנהג).

### אחרי סיום מסלול 3 (לא לפניו)
10. **3.1 — מסכי Owner + LoginScreen ל-theme אחיד.** רפאל המליץ שזה יבוא אחרי 1.5/1.9; עידן מוסיף: גם אחרי שמתן מסיים את עבודת `vehicle_drivers` (מסלול 3), כי זו כרגע המשימה הדחופה/הממוקדת-מוצר ביותר של מתן ולא כדאי לפצל תשומת לב באמצע.
11. **1.9 — איחוד פלטת צבעי סטטוס** (FleetScreen מול `EXPIRY_STYLE`) — יכול להיכנס בין 3 ל-3.1, לפי נוחות תזמון של מתן; אין תלות טכנית קשיחה בסדר בין 1.9 ל-3.1, רק המלצה כללית לסגור לפני שמוסיפים עוד מסכי סטטוס עתידיים (הוצאות/תאונות מהבקלוג).
12. שאר תיקוני 1.7, 1.8, 1.10 — batch נמוך-עדיפות, אין דחיפות בסדר.

---

## תרשים תלות מסוכם

```
מיידי, ללא תלות (מקביל זה לזה):
  דניאל: 1.11, 1.12, 1.13
  מתן:   1.5 → 1.6
  (תנאי סף נפרד, לא תלוי בדבר: קומיט Frontend — סעיף 3.4)

רועי, שלב 2א (אחרי אישור B, מיידי):
  1.1 (RLS fix) + 1.2 + 1.3 + 1.4 + 3.3 (אימות 29/30) — מיגרציה/ישיבה אחת

רועי, שלב 2ב (אחרי 2א):
  2.1 — עיצוב+ביצוע vehicle_drivers

מתן, מסלול 3 (אחרי 1.5/1.6 של מתן, וגם אחרי 2ב של רועי — התלות הכבדה ביותר בתוכנית):
  2.2 + 2.3 → 2.4

מתן, אחרי מסלול 3:
  3.1 (מסכי Owner/Login) — ו/או 1.9 (איחוד פלטה) לפי נוחות תזמון

לא דחוף / דורש אישור נפרד:
  3.2 (Sentry/eas.json) — ממתין לאישור עקרוני נפרד של דור
  2.5 (הוצאות/תאונות — פיצ'רים מהבקלוג) — אחרי כל הנ"ל, לא לפתוח עוד חזית DB במקביל
```

## מה חדש מול טיוטת רפאל (השינויים שעידן הכניס)

- שני הפריטים בסעיף 0 של רפאל (החלטה A והחלטה B) עברו מ"ממתין לאישור" ל"מאושר" — הוסר החסם הפורמלי.
- נקבע סדר מפורש **בתוך** מסלול רועי (2א לפני 2ב) שרפאל לא קבע במפורש (הוא רק אמר ש-1.1-1.4 יכולים לרוץ יחד, ושה-2.1 תלוי באישור — אבל לא סידר את שניהם ברצף אחד מול השני).
- הוברר במפורש: 2.4 (מסכי הנהג) הוא כעת שינוי מחייב, לא "תיקון-ביטחון-קטן-אם-הוחלט-אחרת" כפי שרפאל ניסח כאפשרות ב', כי ההחלטה כבר נפלה לטובת ריבוי-נהגים.
- 3.1 (מסכי Owner) הוזז לאחרי מסלול 3 של מתן (לא רק אחרי 1.5/1.9 כפי שרפאל הציע) — כדי לא לפצל את מתן בין עבודת UX תלוית-DB דחופה לבין ניקוי theme רחב-היקף בו-זמנית.

## שאלות פתוחות שנותרו — עודכן 2026-08-24 (סבב סגירה)

- **מבנה מדויק סופי ל-`vehicle_drivers` (עמודות/אינדקסים) — סטטוס: המלצת מוצר סגורה, ממתין רק לאישור טכני של רועי (לא של עידן).** ההמלצה המלאה מתועדת כעת ב-`.claude/prds/vehicle-drivers-multi.md` (סעיף "דרישות פונקציונליות" + סעיף "החלטות מוצר"): טבלת `vehicle_drivers(vehicle_id, driver_id, is_primary, assigned_at, unassigned_at)`, `is_primary` עם partial unique index פעיל, `unassigned_at` להיסטוריה/סימון הסרה רכה, RLS לפי `driver_id = auth.uid()` לנהג ו-`company_id` (דרך `vehicles`) לאדמין. בנוסף סוכמו עם דור שני פרטים משלימים שלא היו בטיוטה המקורית: (1) רכב יכול להישאר זמנית **בלי** נהג ראשי, לא נדרש מיידית להחליף; (2) **מגבלה של עד 2 נהגים לרכב** (ראשי + משני אחד), שצריכה להיאכף גם ב-UI וגם, אם אפשרי, באילוץ/טריגר ב-DB. אלה נכנסו ל-PRD אך רועי עדיין צריך לאשר טכנית שהמימוש (partial unique index, אכיפת המגבלה) ניתן ליישום כפי שתואר לפני כתיבה בפועל של המיגרציה.
- **האם לכתוב PRD מלא ל-`vehicle-drivers-multi` לפני שרועי מתחיל 2.1 — נענה.** PRD מלא קיים ב-`.claude/prds/vehicle-drivers-multi.md`, כולל User Stories וקריטריוני קבלה לכל ארבעת המסכים הרלוונטיים, "מחוץ לתחום", ותלות טכנית מפורשת. רועי ומתן יכולים לעבוד מולו כ-spec סופי; אין עוד צורך לחכות לניסוח נוסף. סעיף זה נסגר.
