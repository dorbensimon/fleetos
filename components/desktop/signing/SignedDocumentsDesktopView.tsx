/**
 * The signed-documents screens are desktop web only (see the .web.tsx file).
 * This stub keeps pdf.js and the DOM editor out of the phone app bundle.
 */
export function SignedDocumentsDesktopView(_props: { companyId: string }) {
  return null;
}
