# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

# Project safety and collaboration

- Work directly in the local workspace and preserve unrelated user changes.
- Never run `git commit` or `git push` without Dor's explicit approval for that specific action.
- Never deploy to production, apply a production database migration/write, or send an external email/message without Dor's explicit approval.
- Read-only inspection and local file edits/tests requested by Dor are allowed. Ask before installing dependencies or performing destructive actions.
- Never expose secrets, service-role keys, access tokens, or personal data in logs or reports.
- Agents share the same workspace. Respect each agent's ownership boundaries and do not revert another agent's changes.
