# הנחיות ל-Claude Code — התאמת FleetOS למחשב תוך שמירה על גרסת האייפון

## המשימה

עבוד ישירות על פרויקט FleetOS הקיים והפוך את ממשק ה-Web שלו לרספונסיבי ומקצועי למסכי מחשב, טאבלט וטלפון.

זהו שדרוג Frontend רספונסיבי של המוצר הקיים — לא בנייה מחדש, לא יצירת מערכת מקבילה ולא שינוי של הלוגיקה העסקית.

המטרה היא שבמסך מחשב FleetOS ייראה כמו מערכת SaaS מלאה לניהול צי רכב, ינצל נכון את רוחב המסך ויהיה נוח לעבודה עם עכבר ומקלדת. במקביל, כל מסך ברוחב iPhone חייב להישאר זהה ככל האפשר לעיצוב ולהתנהגות הנוכחיים.

לפני שינוי קוד:

1. קרא את `AGENTS.md` ופעל לפיו.
2. קרא את התיעוד המדויק של Expo שמוגדר ב-`AGENTS.md`.
3. עבור על `App.tsx`, `navigation/types.ts`, `lib/theme.ts`, כל הקבצים תחת `screens/`, והרכיבים הרלוונטיים תחת `components/`.
4. אל תנחש מסכים, שדות או הרשאות. הקוד הקיים הוא מקור האמת.
5. בדוק את מצב Git לפני כל שינוי ושמור כל שינוי קיים שאינו שלך.
6. אין לבצע commit, push, deploy, שינוי מסד נתונים, migration, התקנת חבילות או פעולה הרסנית ללא אישור מפורש.

## מהו FleetOS

FleetOS, הממותג בממשק כ-Tolvex, הוא מוצר לניהול חברות וצי רכב. הוא מרכז:

- חברות, מנהלי מערכת ונהגים.
- רכבים, שיוך נהגים לרכבים ומחלקות ארגוניות.
- רישיונות, ביטוחים, בדיקות, טיפולים ותאריכי תוקף.
- תיקי נהג ותיקי רכב, העלאת קבצים וצילומים.
- יצירת תבניות חתימה, שליחת מסמכים לנהגים וחתימה דיגיטלית דרך DocuSeal.
- התראות אישיות, תזכורות, משימות שדורשות טיפול ויומן פעולות.
- הפקת דוחות PDF ושיתוף/הורדתם.

הממשק כולו הוא RTL ובעברית. יש לשמור על RTL, על כיוון טקסט נכון ועל יישור מתאים. נתונים כמו אימייל, טלפון, מספר רישוי, תעודת זהות, VIN וקילומטרים צריכים להישאר LTR בתוך הממשק העברי.

## טכנולוגיה קיימת

- React Native + React Native Web.
- Expo.
- TypeScript.
- React Navigation Native Stack עם כתובות Web מוגדרות.
- Supabase עבור Auth, נתונים, Storage, Edge Functions ו-Realtime.
- DocuSeal עבור תבניות וחתימה דיגיטלית.
- Ionicons בלבד לאייקונים; אין להוסיף emoji.
- פונטים Assistant ו-Heebo כבר נטענים באפליקציה.
- קיימים רכיבי Blur, LinearGradient, Print, Sharing, Image Picker, Document Picker ו-Notifications.

אל תחליף את ה-stack, אל תעביר את הפרויקט ל-Next.js ואל תיצור אפליקציית Web נפרדת. יש לבצע את ההתאמה בתוך הקוד המשותף הקיים, בעזרת `Platform.OS === 'web'`, `useWindowDimensions`, media queries או שכבת responsive משותפת שמתאימה ל-React Native Web.

## סוגי משתמשים והרשאות

### Owner — בעל המערכת

מטרתו לנהל את החברות במערכת.

הרשאות עיקריות:

- לראות את כל החברות.
- לחפש ולסנן חברות פעילות או מושבתות.
- ליצור חברה חדשה יחד עם האדמין הראשון שלה.
- לערוך פרטי חברה.
- להעלות או להחליף לוגו.
- להפעיל, להשבית או למחוק חברה באישור מפורש.
- לראות משתמשי חברה, לערוך משתמש, להוסיף אדמין, להסיר משתמש ולאפס סיסמה.
- להיכנס לאזור מסמכים לחתימה של חברה כאשר `companyId` נמסר למסך.

### Admin — מנהל חברה/צי

מטרתו לנהל את הנהגים, הרכבים, המסמכים, התוקפים והפעילות של החברה שלו בלבד.

הרשאות עיקריות:

- ניהול מלא של נהגים ורכבים של החברה.
- שיוך כמה נהגים לרכב וכמה רכבים לנהג, כולל נהג ראשי.
- ניהול מסמכים, תוקפים, בדיקות, טיפולים ומחלקות.
- יצירת תבניות חתימה ושליחתן לנהגים.
- צפייה בהתראות, משימות שדורשות טיפול, יומן פעולות ודוחות.
- ארכוב ושחזור; מחיקה לצמיתות רק במסכים ובתהליכים שכבר מאפשרים זאת.

### Driver — נהג

מטרתו לראות ולטפל רק במידע האישי וברכבים המשויכים אליו.

הרשאות עיקריות:

- דף בית אישי עם הרכב, משימות תוקף/תחזוקה, רישיון ומסמכים לחתימה.
- צפייה ברכבים המשויכים אליו ועדכון קילומטראז' בלבד, ללא הורדת הערך הקיים.
- צפייה בתיק ובמסמכים האישיים והעלאה רק בקטגוריות המותרות.
- העלאת/החלפת צילום קדמי ואחורי של רישיון נהיגה ועדכון תוקף בהתאם לזרימה הקיימת.
- חתימה על מסמכים וצפייה במסמכים שכבר נחתמו.
- עריכת שם וטלפון בפרופיל האישי בלבד; שאר הנתונים לקריאה בלבד לפי הקוד הקיים.
- ניהול העדפות ההתראות של עצמו בלבד.
- אין להציג פעולות מנהל כמו איפוס סיסמת נהג, ארכוב נהג, מחיקת נהג, יצירת נוהל 6 או ייצוא דוחות מנהל.

## כניסה וניתוב לפי תפקיד

מסך הכניסה כולל מייל וסיסמה, הצגה/הסתרה של הסיסמה, שגיאת התחברות ומצב טעינה.

לאחר התחברות:

- Owner עובר ל-`OwnerHome`.
- Admin עובר ל-`AdminHome`.
- Driver עובר ל-`DriverHome`.
- משתמש עם `must_change_password=true` חייב לעבור קודם למסך `SetPassword`.
- משתמש ללא חברה, עם תפקיד לא תקין, נהג בארכיון או משתמש בחברה מושבתת לא יקבל גישה.

מסך קביעת/שינוי סיסמה כולל:

- סיסמה חדשה, מינימום 8 תווים.
- אימות סיסמה.
- הצגה/הסתרה.
- מצב חובה בכניסה ראשונה ומצב שינוי וולונטרי מהפרופיל.
- ביטול או התנתקות בהתאם להקשר.

## מפת הניווט וה-URLs הקיימים

שמור על כל שמות הנתיבים והפרמטרים הקיימים:

- `/login` — כניסה.
- `/set-password` — קביעת/שינוי סיסמה.
- `/owner` — מסך החברות של Owner.
- `/companies/:companyId` — פרטי חברה.
- `/fleet` — מסך הצי הראשי של Admin.
- `/drivers/:driverId` — תיק נהג.
- `/drivers/edit/:driverId?` — יצירה/עריכת נהג.
- `/drivers/archive` — ארכיון נהגים.
- `/drivers/:driverId/personal-details` — פרטי נהג.
- `/drivers/:driverId/license-documents` — מסמכי רישיון.
- `/vehicles/:vehicleId` — תיק רכב.
- `/vehicles/edit/:vehicleId?` — יצירה/עריכת רכב.
- `/departments` — מחלקות.
- `/activity-log` — יומן פעולות.
- `/attention` — דורש טיפול.
- `/reports` — ייצוא דוחות.
- `/admin/profile` — פרופיל מנהל.
- `/notifications` — התראות.
- `/notification-preferences` — ניהול התראות.
- `/documents/signing` — ניהול מסמכים לחתימה.
- `/documents/view` — עורך/חתימה/תצוגת מסמך או תמונה.
- `/documents/:ownerType/:ownerId/:category` — קטגוריית מסמכים.
- `/driver` — בית הנהג.
- `/my-vehicle` — הרכבים שלי.
- `/my-vehicle/odometer/:vehicleId` — עדכון קילומטראז'.
- `/my-documents` — המסמכים שלי.
- `/my-documents/signing` — מסמכים לחתימה.
- `/my-profile` — הפרטים שלי.
- `/menu` — תפריט משותף מותאם תפקיד.

כפתורי Back בדפדפן, כפתורי חזרה בממשק ו-URL ישיר חייבים לעבוד. אין להחליף navigation push ב-state מקומי כאשר זה ישבור את היסטוריית הדפדפן. החריג הקיים הוא המעבר בין נהגים לרכבים בתוך מסך הצי, שהוא מצב פנימי של אותו מסך.

## כללי עיצוב קיימים שיש לשמר

- שפת המותג הראשית: כחול `#0088CC`.
- רקע אדמין: `#F1F4F7` עם הגרדיאנט הקיים.
- טקסט ראשי: `#102A42` או הטוקנים הקיימים של אותו מודול.
- כרטיסים לבנים/זכוכית, פינות מעוגלות וצללים רכים.
- צבעי מצב: ירוק לתקין, כתום לקרוב/אזהרה, אדום לפג/שגיאה ואפור לחסר.
- במסכי תיק נהג משתמשים בשפה דמוית iOS ובפונט Heebo לפי `driverCardTheme.ts`.
- במסך הצי ובבית הנהג משתמשים ב-Hero כחול ואלמנטים מזכוכית לפי הקוד הקיים.
- במסכי שאר האדמין משתמשים בטוקנים של `lib/theme.ts`.
- אין לאחד באופן עיוור ערכות נושא שכבר הופרדו בכוונה; כן יש ליצור כללי פריסה רספונסיביים משותפים.

## אסטרטגיית רספונסיביות נדרשת

### נקודות שבירה

- Mobile: עד 767px. לשמור על המראה וההתנהגות הקיימים כמעט ללא שינוי.
- Tablet: 768–1199px. תוכן רחב יותר, שתי עמודות במקומות מתאימים ומודלים ממורכזים.
- Desktop: 1200px ומעלה. מעטפת SaaS מלאה עם ניווט צדדי ותוכן שמנצל את המסך.

### מעטפת Desktop

ב-Web וברוחב 768px ומעלה צור מעטפת תפקידית יציבה:

- Sidebar ימני בגלל RTL, ברוחב כ-240–280px ב-Desktop וכ-80–96px במצב מצומצם אם נדרש.
- בחלק העליון של ה-Sidebar: לוגו Tolvex, שם החברה ותפקיד המשתמש.
- פריטי ניווט עם אייקון, טקסט, מצב פעיל ו-badge כאשר יש מונה.
- Top bar לתוכן עם כותרת מסך, breadcrumbs או פעולות הקשר, פעמון התראות ואזור משתמש.
- אזור תוכן מרכזי ברוחב שימושי של 1180–1440px, עם שוליים נדיבים.
- במסכים שזקוקים לכך, פאנל משני שמאלי לפרטי עזר, סטטוסים או פעולות.
- ב-Mobile אין Sidebar; התפריט הקיים וכפתורי החזרה נשארים.

ניווט מוצע לפי תפקיד:

- Owner: חברות, פרטי המשתמש/התנתקות לפי הקיים.
- Admin: צי נהגים ורכבים, דורש טיפול, מסמכים לחתימה, דוחות, מחלקות, יומן פעולות, התראות, הגדרות/פרופיל.
- Driver: בית, הרכב שלי, המסמכים שלי, מסמכים לחתימה, התראות, ניהול התראות, הפרטים שלי.

אל תיצור הרשאה חדשה רק משום שקישור מופיע ב-Sidebar. ה-Sidebar חייב להיבנות לפי `profile.role` ולהציג רק נתיבים שהמשתמש רשאי לפתוח.

### התנהגות Desktop כללית

- אין למתוח טופס צר על כל רוחב המסך.
- טפסים: עמודת טופס ראשית ברוחב 720–900px ופאנל סיכום/עזרה של 280–360px כאשר יש ערך בכך.
- רשימות: Grid של 2–3 עמודות או טבלה/רשימה רחבה בהתאם לסוג המידע.
- תיקי נהג ורכב: Hero קומפקטי, פעולות מהירות בשורה, ותוכן מפוצל ל-2 עמודות.
- Modal/Bottom Sheet: במחשב יוצג כ-Dialog ממורכז, לא כפאנל שצמוד לתחתית המסך. בטלפון יישאר Bottom Sheet.
- Select, Date picker ותפריטי פעולה צריכים להיפתח ליד השדה במחשב ולהישאר נגישים למקלדת.
- פעולות שמופעלות ב-long press בטלפון חייבות לקבל כפתור גלוי או תפריט הקשר במחשב.
- Hover, focus-visible, cursor ו-tooltip צריכים להופיע באלמנטים לחיצים ב-Web.
- אין ליצור גלילה כפולה. מעטפת האפליקציה יכולה להיות בגובה viewport, ורק אזור התוכן הראשי או הרשימה הפעילה יגללו.
- Headers ופעולות חשובות יכולים להיות sticky במחשב.
- שמור על safe area בטלפון, אך אל תוסיף רווחים המדמים notch ב-Web Desktop.

## מסכי Owner

### 1. רשימת חברות — OwnerHome

מטרה: ניהול כל החברות במערכת.

מידע ופעולות:

- שם המוצר Tolvex.
- מונה חברות פעילות מתוך כלל החברות.
- חיפוש לפי שם חברה.
- סינון: הכל, פעיל, מושבת.
- כרטיס חברה עם לוגו, שם, סטטוס, מספר אדמינים ומספר נהגים.
- פתיחת חברה.
- תפריט פעולות: הפעלה/השבתה ומחיקה.
- כפתור חברה חדשה.
- התנתקות.

ב-Desktop הצג את החברות כ-Grid של 2–4 כרטיסים לפי הרוחב או כטבלה עשירה. שורת החיפוש, הסינון וכפתור הוספה יהיו באותו Toolbar. שמור Empty, Loading, Error ו-Refresh.

### 2. יצירת חברה

שדות:

- שם חברה — חובה.
- לוגו — אופציונלי, PNG/JPG.
- סוג חברה — בע״מ או עוסק מורשה.
- ח.פ / ע.מ — אופציונלי.
- שם פרטי של האדמין הראשון — חובה.
- שם משפחה של האדמין הראשון — חובה.
- מייל אדמין — חובה ותקין.
- טלפון — חובה וטלפון ישראלי תקין.
- סיסמה זמנית — חובה, לפחות 4 ספרות בלבד.
- אימות סיסמה — חובה וזהה.

לאחר יצירה, האדמין מתחבר עם הסיסמה הזמנית ונדרש לקבוע סיסמה קבועה.

ב-Desktop הצג Dialog רחב עם שתי עמודות: פרטי חברה ופרטי אדמין ראשון. אזור הפעולות קבוע בתחתית ה-Dialog.

### 3. פרטי חברה — CompanyDetail

שדות חברה:

- שם חברה.
- לוגו.
- סוג חברה.
- ח.פ / ע.מ.
- כתובת.
- טלפון חברה.
- שם קצין רכב.
- נייד קצין רכב.
- סטטוס פעיל/מושבת.

פעולות:

- שמירת שינויים.
- הפעלה/השבתה.
- מחיקה לאחר הקלדת שם החברה.
- רשימת מנהלים ונהגים.
- הוספת אדמין: שם פרטי, שם משפחה, מייל, טלפון, סיסמה ואימות.
- עריכת שם וטלפון של משתמש.
- הסרת משתמש באישור.
- איפוס סיסמת משתמש: סיסמה חדשה ואימות.
- מעבר למסמכים לחתימה של החברה.

ב-Desktop השתמש בפריסת Master-detail: פרטי החברה בפאנל אחד, רשימת המשתמשים בפאנל שני. פעולות מסוכנות ירוכזו באזור נפרד וברור.

## מסכי Admin

### 1. מסך הצי — AdminHome / FleetScreen

מטרה: מרכז העבודה היומי של מנהל הצי.

המסך כולל מצב נהגים ומצב רכבים באותו Route, עם מעבר ביניהם ללא push למסך חדש.

מצב נהגים:

- סטטיסטיקות: סה״כ נהגים, רישיונות קרובים לפוג, רישיונות שפגו.
- חיפוש לפי שם, תעודת זהות, מספר עובד או טלפון.
- סינון: הכל, רישיון קרוב לפוג, רישיון פג, ללא רכב.
- קישור לארכיון נהגים עם מונה.
- כרטיס נהג: שם, דרגת רישיון, סטטוס ותוקף רישיון, רכב משויך, מסמכים הממתינים לחתימה, מצב הפעלת חשבון וכפתור חיוג.
- פתיחת תיק הנהג, פתיחת הרכב המשויך וחיוג.
- הוספת נהג.

מצב רכבים:

- סטטיסטיקות: סה״כ רכבים, פעילים, מושבתים/בטיפול.
- חיפוש לפי מספר רישוי, דגם, יצרן, קוד פנימי או שם נהג.
- סינון: הכל, פעיל, בטיפול, מושבת, בארכיון.
- כרטיס רכב: לוחית, יצרן ודגם, סוג, מחלקה, נהג ראשי ומספר נהגים נוספים, סטטוס כולל, ביטוח, טסט וטיפול הבא.
- שחזור רכב מהארכיון.
- פתיחת תיק רכב.
- הוספת רכב.

פעולות משותפות:

- תפריט, התראות, דורש טיפול ויומן פעולות.
- Refresh, Loading skeleton, Empty ו-Error.

ב-Desktop:

- אל תשאיר את ה-Hero וה-sheet בפריסה צרה של טלפון.
- הצג Header/Toolbar אופקי עם בורר נהגים/רכבים, חיפוש, פילטרים, כפתור הוספה ופעולות משניות.
- סטטיסטיקות יהיו 3–4 כרטיסי KPI בשורה.
- אפשר לבחור בין Grid של כרטיסים לבין רשימה/טבלה; העדף תצוגה שמאפשרת לסרוק הרבה נתונים בלי לאבד את צבעי הסטטוס והפעולות הקיימות.
- ב-Tablet ניתן להשתמש בשתי עמודות; ב-Desktop בשלוש עמודות או טבלה מלאה.
- אין צורך ב-Dock צף תחתון במחשב; הבורר והפעולות צריכים לעבור ל-Toolbar.

### 2. יצירה ועריכת נהג — DriverForm

שדות פרטים אישיים:

- שם מלא — חובה.
- טלפון — חובה וטלפון ישראלי תקין.
- תעודת זהות — חובה ביצירה, 9 ספרות עם בדיקת תקינות; בעריכה רשומה ישנה יכולה להישאר חסרה.
- מספר עובד — אופציונלי.
- מחלקה — אופציונלי.

שדות רישיון:

- דרגת רישיון ראשית — חובה ביצירה.
- דרגת רישיון נוספת — אופציונלי ולא זהה לראשית.
- תוקף רישיון — חובה ביצירה וחייב להיות תאריך עתידי כאשר מוזן.

דרגות נתמכות כוללות A/A1/A2, B, C/C1/C+E, D/D1/D2/D3, 1(T) והיתר מכונה ניידת, בהתאם לרשימה בקוד.

שדות גישה ביצירת נהג:

- מייל — חובה ותקין.
- סיסמה זמנית — חובה, לפחות 4 ספרות בלבד.
- הצגה/הסתרת סיסמה.
- מתג שליחת הזמנה ב-SMS.

בעריכה המייל מוצג לקריאה בלבד במסך זה. קיימים Progress bar, מצבי שגיאה, ספירת שדות חובה וכפתור יצירה/שמירה.

ב-Desktop פצל את הטופס לשתי עמודות: פרטים אישיים וגישה מצד אחד, רישיון וסיכום מצד שני. שמור CTA דביק וברור. אין לשנות ולידציות או payload.

### 3. תיק נהג — DriverDetail

מטרה: תצוגת 360 מעלות על הנהג.

Hero ופעולות מהירות:

- שם, אות אווטאר, סטטוס, דרגת רישיון ורכב.
- עריכת נהג.
- התקשרות, הודעה ופתיחת רכב משויך.
- אישור או דחיית בקשת עדכון רישיון כאשר קיימת.

קבוצות מידע:

- פרטי קשר ורכב: טלפון, אימייל, תעודת זהות ורכב/רכבים משויכים.
- מסמכים ורישוי: מסמכי רישיון נהיגה, מסמכים לחתימה, מסמכים כלליים ומידע תעבורתי.
- תיק נהג ותקשורת: תיק נהג, הערות ותגובות, דוחות תעבורה ונהגים נלווים.
- בטיחות והדרכות: נוהל 6, הסמכות והכשרות, חומרים מסוכנים והדרכות.
- דוחות: ייצוא דוח תמונת מצב של הנהג.
- ניהול חשבון: איפוס סיסמת נהג.

פעולות ניהול:

- שינוי כתובת המייל דרך הזרימה הייעודית.
- איפוס סיסמה זמנית ואילוץ קביעת סיסמה קבועה.
- העברה לארכיון וחסימת גישה.
- שחזור כאשר הנהג בארכיון.

ב-Desktop הצג Hero קומפקטי בראש, Quick actions אופקיות, ואז Grid של שתי עמודות לקבוצות. אל תיצור גרסה שונה לנהג עצמו: `DriverDocuments` צריך להשתמש באותה שפה חזותית ובאותם רכיבי תיק, עם סינון פעולות לפי הרשאה.

### 4. פרטי נהג — DriverPersonalDetails

תצוגת שדות:

- שם מלא, חברה, מייל להתחברות, טלפון, תעודת זהות, מספר עובד, מחלקה, דרגת רישיון, תוקף רישיון ותאריך הצטרפות.
- כפתור עריכה.
- ניהול כל הרכבים המשויכים לנהג: צפייה, הוספה והסרת שיוך.
- הנהג הראשון ברכב יכול להפוך לראשי בהתאם ללוגיקה הקיימת.
- אסור ששיתוף רכב חדש ימחק שיוך קיים.

### 5. ארכיון נהגים

- רשימת נהגים בארכיון.
- שם, תאריך ארכוב ושם המשתמש שביצע את הארכוב.
- פתיחת התיק.
- שחזור.
- מחיקה לצמיתות רק לאחר הקלדת שם הנהג.
- המחיקה כוללת את תיק הנהג והקבצים לפי הזרימה הקיימת; מסמכים חתומים נשמרים לפי כללי המערכת.

### 6. יצירה ועריכת רכב — VehicleForm

שדות זיהוי:

- מספר רישוי — חובה, 7–8 ספרות.
- חיפוש במאגר רכב לפי מספר רישוי ומילוי אוטומטי של פרטים כאשר נמצא.
- סוג רכב — חובה: פרטי, מסחרי/מיניבוס, משא או אוטובוס לפי הערכים הקיימים.
- יצרן.
- דגם.
- צבע.
- קוד פנימי.
- VIN — אופציונלי; כאשר מוזן חייב להיות 17 תווים חוקיים ללא I/O/Q.
- קילומטראז' נוכחי.
- חודש ושנת ייצור; שנה בין 1980 לשנה הנוכחית.
- תאריך עלייה לכביש, שלא יכול להיות לפני שנת הייצור.
- תוקף רישיון רכב.

שדות שיוך וסטטוס:

- סטטוס חובה: פעיל, בטיפול או מושבת. ארכיון מנוהל בזרימת הארכוב.
- מחלקה.
- שימוש הרכב.
- סוג עסקה: רכישה, ליסינג או השכרה.
- נהגים משויכים: רשימה, הוספה, הסרה וסימון ראשי בהתאם ללוגיקה הקיימת. ביצירת רכב יש לשמור אותו לפני שיוך נהגים.

ב-Desktop סדר את שדות הזיהוי ב-Grid של שתי עמודות. השאר את מספר הרישוי והחיפוש בשורה בולטת. אזור הנהגים המשויכים צריך להיות רחב, מרווח ולא צמוד לתחתית הכרטיס.

### 7. תיק רכב — VehicleDetail

Tabs קיימים:

- כללי.
- נהג משויך.
- תחזוקה.
- מסמכים.

כללי:

- שם הרכב, מספר רישוי, סוג וסטטוס.
- ביטוח, טסט, טיפול הבא ומד אוץ.
- מספר רישוי, יצרן ודגם, צבע, סוג, שנת ייצור ועלייה לכביש.
- VIN, קוד פנימי, מחלקה, שימוש הרכב וסוג עסקה.
- כפתורי מחיקת רכב והעברה/הסרה מארכיון מופיעים רק ב-Tab הכללי.

תחזוקה:

- מד אוץ נוכחי.
- מועד עדכון אחרון.
- קילומטרים בטיפול האחרון.
- טווח קילומטרים בין טיפולים.
- קילומטרים לטיפול הבא.
- עריכה, שמירה וביטול.

נהגים משויכים:

- רשימת נהגים פעילים ברכב.
- הוספת נהג, הסרת שיוך, נהג ראשי ופתיחת תיק הנהג.
- שינויים מתעדכנים ב-Realtime.

מסמכים ובדיקות:

- שימוש ב-`ComplianceSection` ובקטלוג התוקפים הקיים.

ב-Desktop Tabs יהיו פס לשוניות גלוי. ב-Tab הכללי פצל את הקבוצות לשתי עמודות. פעולות מסוכנות יישארו באזור ייעודי בתחתית Tab כללי בלבד.

### 8. תוקפים ומסמכי רכב

קטלוג רכב:

- רישיון רכב.
- רישיון הפעלה.
- ביטוח חובה.
- ביטוח מקיף.
- טסט שנתי; ניתן לחשב תוקף שנה מתאריך בדיקה אחרונה.
- בדיקת בלמים חצי שנתית; ניתן לחשב 183 ימים.
- בדיקת חורף.
- בדיקת שכחת ילדים.
- טכוגרף.
- ביקורת קצב״ת.

לכל פריט יכולים להיות:

- תאריך בדיקה אחרונה.
- תאריך תוקף/בדיקה הבאה.
- תג מצב תקין/קרוב/פג/חסר/אופציונלי.
- מסמך אחד או יותר.
- העלאה ממצלמה, גלריה או קובץ.
- צפייה, הורדה ומחיקה באישור.
- שינוי תאריכים נשמר רק לאחר אישור, לא מיד בבחירת תאריך.

### 9. מחלקות

- יצירת מחלקה לפי שם.
- רשימת מחלקות.
- שינוי שם inline.
- מחיקה באישור.
- לפני מחיקה להציג כמה נהגים ורכבים משויכים אליה ולהבהיר שהם יישארו ללא מחלקה.

### 10. דורש טיפול

ארבעה סוגי סיכום:

- רישיונות נהיגה שפגו או יפוגו בתוך 30 יום.
- רכבים ללא ביטוח חובה בתוקף.
- רכבים ללא נהג משויך.
- נהגים ללא צילום רישיון מלא או ללא תוקף רישיון.

ב-Desktop הצג KPI כולל וארבעה כרטיסי קטגוריה בשורה/Grid. שמור מצב "הכול מטופל".

### 11. יומן פעולות

- מציג את 100 הפעולות האחרונות בחברה.
- פעולות: נוצר, עודכן, נמחק.
- ישויות: מחלקה, רכב, תיק נהג, פרופיל, פריט עמידה בדרישות, מסמך, שיוך, תבנית חתימה ובקשת חתימה.
- בכל שורה: תיאור, מבצע, סוג ישות ותאריך/שעה.

ב-Desktop הצג טבלה או Timeline רחב עם עמודות ברורות, תוך שמירת הנוסח המפורט הקיים.

### 12. דוחות

דוחות נהגים:

- כל הנהגים.
- רישיון קרוב לפוג.
- רישיון פג תוקף.
- ללא רכב משויך.

עמודות בדוח: שם מלא, ת.ז, טלפון, דרגת רישיון, תוקף, סטטוס ורכב משויך.

דוחות רכבים:

- כל הרכבים.
- רכבים עם חריגה.
- ביטוח פג/קרוב לפוג.
- טסט פג/קרוב לפוג.
- טיפול קרוב/באיחור.

הדוחות נוצרים כ-PDF ויורדים או משותפים בהתאם לפלטפורמה. אל תשנה את תוכן הדוח או מנגנון הייצוא כחלק משינוי העיצוב.

### 13. פרופיל Admin

- אימייל — קריאה בלבד.
- טלפון — ניתן לעריכה.
- שם מלא — ניתן לעריכה.
- תפקיד — קריאה בלבד.
- שם חברה, סוג חברה, ח.פ/ע.מ וכתובת — קריאה בלבד במסך זה.
- טלפון חברה — ניתן לעריכה.
- שינוי סיסמה.
- תאריך הצטרפות.
- התנתקות עם הודעת אישור.

## מסמכים וחתימות — Admin

### מסך מסמכים לחתימה

Tabs:

- תבניות.
- נשלחו.
- ארכיון.
- הוסרו — לאדמין בלבד, עבור מסמכים חתומים שהוסרו אך נשמרים כראיה.

יצירת תבנית:

- שדה שם מסמך.
- בחירת מקור: מצלמה, גלריה או קובץ.
- תמונה מומרת ל-PDF תוך שמירת יחס התמונה.
- פתיחת עורך DocuSeal למיקום שדות החתימה.
- תצוגה מקדימה, הורדת תבנית, ארכוב ושליחה.

שליחה:

- בחירת נהג אחד או יותר.
- יצירת בקשת חתימה לכל נהג.
- מעבר לרשימת נשלחו.
- תמיכה בהצלחה חלקית והצגת כמות שנשלחה.

נשלחו:

- שם מסמך, שם נהג, תאריך, סטטוס ממתין/חתום.
- פתיחת מסמך חתום.
- הורדת מסמך חתום.
- שליחת תזכורת ידנית.
- ארכוב.

הגדרות תזכורת במייל:

- הפעלה/כיבוי אוטומטי לכל החברה.
- תזכורת ראשונה אחרי מספר שעות.
- הפרש שעות בין תזכורות.
- מספר תזכורות מרבי.
- ברירת מחדל קיימת: מופעל, 72 שעות, 72 שעות, עד 3 מיילים.
- אין SMS בזרימה זו.

ארכיון:

- שחזור תבנית או בקשה.
- מחיקה לצמיתות לפי ההגבלות הקיימות.
- מסמך חתום שמוסר מהארכיון נשמר בנתונים ובקובץ כראיה.
- מחיקת כל הארכיון זמינה לאדמין עם אישור מפורש.

ב-Desktop:

- הצג Tabs אופקיים ו-Toolbar עם יצירת תבנית והגדרות.
- רשימת תבניות ובקשות יכולה להיות טבלה עם פעולות גלויות.
- בחירת נהגים תהיה Dialog ממורכז עם חיפוש, checkbox, בחירה מרובה וכפתור שליחה.
- עורך/תצוגת DocuSeal צריך לנצל את גובה ורוחב ה-viewport, עם Header קומפקטי וכפתור חזרה תקין.

## מסכי Driver

### 1. בית הנהג — DriverHome

מטרה: להראות לנהג מיד מה דורש ממנו פעולה.

המסך כולל:

- ברכה לפי השעה ושם הנהג.
- תפריט ופעמון התראות עם נקודה כאשר יש התראות שלא נקראו.
- הרכב הראשי/המשויך: יצרן, דגם, מספר רישוי וסוג.
- כרטיס תוקף ותחזוקה עם עד שלוש משימות בעדיפות לפי חומרה.
- מסמכים הממתינים לחתימה.
- סטטוס ותוקף רישיון נהיגה.
- פרטי מנהל הצי וכפתורי התקשרות והודעה.
- מצב ברור כאשר אין רכב משויך.

ב-Desktop הפוך את ה-Hero לכותרת Dashboard אופקית. הצג כרטיס רכב ראשי, משימות, חתימות ורישיון ב-Grid. פרטי המנהל יכולים להיות פאנל צד קבוע. שמור את אותה היררכיית דחיפות.

### 2. הרכב שלי — DriverVehicle

- נהג יכול להיות משויך ליותר מרכב אחד.
- לכל רכב: מספר רישוי, יצרן ודגם, סוג, סימון ראשי/משני כאשר יש כמה, ביטוח חובה, טסט וקילומטראז'.
- פעולה: עדכון קילומטרים.
- אין פעולות ניהול, שיוך, ארכוב או מחיקה.

### 3. עדכון קילומטראז'

- הצגת הערך הקיים.
- שדה מספרי לערך חדש.
- הערך חייב להיות מספר שלם ובטוח ושווה או גדול מהערך הקיים.
- לאחר שמירה מוצגת הודעה שמנהל הצי יראה את הערך החדש.

### 4. המסמכים שלי — DriverDocuments

המראה צריך להיות זהה לתיק הנהג שמנהל רואה עבור אותו נהג, באמצעות אותם רכיבי Hero וקבוצות. ההבדל הוא הרשאות בלבד.

קבוצות שמוצגות לנהג:

- פרטי קשר ורכב.
- מסמכים ורישוי.
- תיק נהג ותקשורת.
- בטיחות והדרכות.

יש להסיר קבוצות מנהל כמו דוחות וניהול חשבון. השורות מובילות לאותם מסכי מסמכים משותפים, אבל פעולות מנהל לא יוצגו.

קטגוריות מסמכים:

- מסמכי רישיון נהיגה.
- מסמכים לחתימה.
- מסמכים כלליים.
- מסמכי מידע תעבורתי.
- תיק נהג.
- הערות ותגובות.
- דוחות תעבורה.
- נהגים נלווים.
- נוהל 6.
- הסמכות והכשרות.
- חומרים מסוכנים.
- הדרכות והכשרות.

### 5. מסמכי רישיון נהיגה

- צילום צד קדמי וצד אחורי.
- העלאה ממצלמה, גלריה, קובץ או סריקה.
- תצוגה גדולה, הורדה והחלפה.
- סריקה יכולה לזהות תאריך תוקף ולהציע/לעדכן אותו לפי הזרימה הקיימת.
- תוקף רישיון.
- סטטוס מאומת רק כאשר יש שני צדדים ותאריך תוקף.
- מנהל יכול לערוך; נהג רואה רק את הפעולות שהותרו בקוד עבור עצמו.

### 6. קטגוריית מסמכים כללית

- Hero מותאם לקטגוריה ומספר פריטים.
- רשימת קבצים עם שם ותאריך.
- צפייה, הורדה, העלאה ומחיקה בהתאם ל-`allowDelete` ולהרשאות.
- העלאה ממצלמה, גלריה או קובץ.
- נוהל 6 הוא טופס מובנה שיוצר PDF; נהג אינו יוצר נוהל 6 אלא צופה במה שהמנהל יצר.

שדות טופס נוהל 6:

- תאריך אירוע — חובה.
- שעת אירוע — חובה.
- פרטי האירוע — חובה.
- מספר רכב — חובה.
- שם הנהג המתלונן — חובה.
- טלפון הנהג המתלונן — חובה וטלפון ישראלי תקין.
- תמונה/תיעוד — אופציונלי.

### 7. מסמכים לחתימה — DriverSigningDocuments

- Tabs: ממתינים לחתימה ומסמכים חתומים.
- סנכרון סטטוס מול DocuSeal.
- מסמך ממתין/נדחה נפתח לחתימה.
- מסמך שהושלם נפתח לצפייה עם סימון חתום דיגיטלית.
- מוצגים שם התבנית, תאריך וסטטוס.
- כאשר מנהל פותח את המסך מתוך תיק נהג, מוצגים רק המסמכים של אותו נהג.

### 8. הפרטים שלי — DriverProfile

המראה צריך להיות מקביל למסך הפרטים האישיים של מנהל.

- אימייל — קריאה בלבד.
- טלפון — ניתן לעריכה.
- שם מלא — ניתן לעריכה.
- תפקיד נהג — קריאה בלבד.
- חברה, מספר עובד ומחלקה — קריאה בלבד.
- תעודת זהות, דרגת רישיון, תוקף ותאריך הצטרפות — קריאה בלבד במסך זה.
- התנתקות עם שאלת אישור ורק לאחר תשובה חיובית.

## התראות

### מסך ההתראות

- המידע מסונן לפי המשתמש וההרשאה הקיימים.
- התראה לא נקראת מסומנת בנקודה.
- פתיחת התראה מסמנת אותה כנקראה.
- "קרא הכל" מסמן את כולן כנקראו.
- נהג מנותב לפי הסוג למסמכים לחתימה, הרכב שלו או הפרופיל.
- מנהל מנותב לתיק נהג, למסמכים לחתימה או למסך הצי בהתאם למידע הקיים.
- כאשר סכמת ההתראה אינה מחזיקה מזהה רכב, אין להמציא deep-link לרכב מסוים; יש לשמור את הניתוב הקיים למסך הצי.

### העדפות התראות אישיות

כל שינוי נשמר מיד עבור המשתמש הנוכחי בלבד. אין כפתור שמירה כללי. שורה חסרה במסד הנתונים פירושה שההתראה מופעלת כברירת מחדל.

סוגי Admin:

- עדכון פרטי נהג.
- העלאת מסמך נהג.
- תוקף ביטוח חובה.
- תוקף ביטוח מקיף.
- תוקף טסט שנתי.
- תוקף בדיקת רכב לפי בדיקה אחרונה.
- טיפול רכב מתקרב.

סוגי Driver:

- מסמך חדש לחתימה.
- שיוך לרכב.
- המנהל עדכן את פרטי הנהג.
- תוקף בדיקת רכב משויך.

המתג חייב להיראות ולהתנהג בסגנון iOS: מסילה אפורה/ירוקה, thumb לבן, אנימציה חלקה של כ-220ms, focus נגיש במחשב ומצב disabled בזמן שמירה.

## תפריט והתנתקות

התפריט הוא מסך משותף שהפריטים בו משתנים לפי תפקיד:

- Driver: הפרטים שלי, המסמכים שלי, ניהול התראות.
- Owner: הפרטים שלי, מחלקות, ניהול התראות, רק אם המסכים אכן מורשים לפי הקוד.
- Admin: הפרטים שלי, מחלקות, ייצוא דוחות, מסמכים לחתימה עם badge, ניהול התראות.

ב-Desktop פריטים אלה יעברו ל-Sidebar, אך מסך `/menu` חייב להישאר פעיל עבור Mobile וכתובות קיימות.

לחיצה על התנתקות תמיד פותחת הודעת אישור. רק לחיצה על "התנתק" מבצעת `supabase.auth.signOut()` ומחזירה למסך הכניסה. "ביטול" אינו משנה דבר.

## מצבי מערכת שחייבים להישמר בכל מסך

- Loading או Skeleton.
- Empty state עם פעולה או הסבר רלוונטי.
- Error עם Retry.
- Saving/Uploading/Processing עם מניעת לחיצה כפולה.
- Toast לפעולה מוצלחת או שגיאה קלה.
- Dialog אישור לפעולה מסוכנת.
- שדות עם שגיאה inline והודעה ברורה.
- שמירת ערכים שלא נשמרו כאשר שגיאה זמנית מתרחשת.
- רענון הנתונים כאשר חוזרים למסך.
- Realtime בשיוכי נהג-רכב לפי המימוש הקיים.
- פעולות assignment ממתינות ממשיכות להסתנכרן כאשר האפליקציה חוזרת למצב active.

## דרישות נגישות ו-Web

- כל פעולה תהיה נגישה ב-Tab וב-Enter/Space.
- Focus indicator ברור; אין להסיר outline בלי חלופה.
- `accessibilityLabel`, role ו-state קיימים יישמרו.
- שטח לחיצה מינימלי 42–44px.
- ניגודיות טקסט מספקת.
- כותרות, טבלאות, Dialogs ו-checkboxes יקבלו semantics מתאימים ב-Web ככל ש-React Native Web מאפשר.
- אין להסתמך רק על צבע להעברת סטטוס.
- תפריטי Popover ו-Dialog נסגרים עם Escape ושומרים focus תקין.
- שדות LTR בתוך RTL לא יתהפכו או יציגו מספרים בסדר שגוי.
- Chrome, Edge ו-Safari ברוחבי 375, 768, 1024, 1280, 1440 ו-1920 צריכים לעבוד.

## עקרונות מימוש

1. צור שכבת responsive משותפת ולא עשרות תנאים שונים בכל מסך.
2. אפשר ליצור hooks/tokens כגון breakpoints, desktop shell, responsive container, desktop dialog ו-grid helpers — אבל השתמש במבנה הקיים.
3. שמור את `CONTENT_MAX_WIDTH=640` למסכים צרים שבהם הוא מתאים, ואל תשנה אותו גלובלית באופן שימתח את כל מסכי Mobile. הוסף tokens נפרדים ל-Desktop.
4. הימנע מערכים קשיחים שמסתדרים רק ברוחב אחד.
5. אל תשכפל מסכים לפי פלטפורמה אלא אם יש הצדקה טכנית ברורה. העדף רכיבים משותפים עם וריאציות פריסה.
6. אל תשנה queries, payloads, Edge Functions, RLS, שמות טבלאות, שמות שדות, routes או הרשאות.
7. אל תשנה טקסטים עסקיים, ולידציות או flow אלא אם נדרש במפורש כדי לאפשר שימוש במחשב.
8. אל תוסיף dependency לפני בקשת אישור.
9. שמור על ביצועים: רשימות גדולות יישארו `FlatList`/virtualized; אל תהפוך אותן ל-map ארוך רק לצורך Grid.
10. אל תחשוף secrets או נתונים אישיים בלוגים.

## סדר עבודה מומלץ

### שלב 1 — תשתית

- הוסף breakpoints וטוקנים לרוחב Desktop.
- צור `ResponsiveAppShell`, `ResponsiveContainer`, `ResponsiveGrid` ו-`ResponsiveModal` או שמות דומים.
- ודא שה-Shell יודע את role, נתיב פעיל, שם חברה, מונה התראות ופעולת התנתקות.
- שמור את מעטפת Mobile הקיימת מתחת ל-768px.

### שלב 2 — מסכי ליבה

- Admin Fleet.
- Driver Home.
- Owner Home.
- Login ו-Set Password.

### שלב 3 — תיקי פרטים וטפסים

- Driver Detail/Documents/Profile/Form.
- Vehicle Detail/Form.
- Company Detail.

### שלב 4 — מסכים תפעוליים

- מסמכים, רישיון, חתימות ו-DocuSeal.
- התראות והעדפות.
- דוחות, מחלקות, דורש טיפול, ארכיון ויומן פעולות.

### שלב 5 — QA

- בדיקת כל role בנפרד.
- בדיקת URL ישיר וכפתורי Back.
- בדיקת Keyboard/Mouse/Touch.
- בדיקת RTL ו-LTR מעורבים.
- בדיקת כל Dialog, Select, Date picker, File picker ו-DocuSeal.
- בדיקת responsive בכל נקודות השבירה.

## קריטריוני קבלה

- ב-375/390px גרסת האייפון נראית ומתנהגת כמו לפני השינוי.
- ב-768/1024px אין תוכן צר באמצע מסך ריק, אין חיתוך ואין overflow אופקי.
- ב-1280/1440/1920px המערכת נראית כמו Dashboard שולחני מלא ומנצלת את הרוחב באופן מבוקר.
- אין גלילה כפולה ואין אזורים שלא ניתן להגיע אליהם.
- כל כפתורי החזרה עובדים, כולל browser back.
- כל מסך ניתן לפתיחה ב-URL הקיים שלו.
- Sidebar ותפריטים מציגים רק פעולות המותרות לתפקיד.
- מסכי Driver שמקבילים למסכי Admin משתמשים באותה שפה חזותית, כשההבדל הוא הרשאות בלבד.
- ארכוב, מחיקה והתנתקות תמיד מבקשים אישור לפי הזרימה הקיימת.
- פעולות מסוכנות בתיק רכב מוצגות רק ב-Tab כללי.
- כל הטפסים שומרים את אותם שדות וולידציות.
- מסמכים, סריקת רישיון, PDF, שיתוף, הורדה ו-DocuSeal עובדים ב-Web.
- `npm run typecheck`, `npm run lint` והבדיקות הרלוונטיות עוברים ללא שגיאות חדשות.
- אין שינוי DB, dependency, commit, push או deploy ללא אישור מפורש.

## פלט נדרש ממך

לפני תחילת העריכה, החזר תכנית קצרה עם:

- הרכיבים המשותפים שתיצור או תשנה.
- המסכים שתעביר בכל שלב.
- הסיכונים לשמירת Mobile ו-RTL.

לאחר מכן בצע את העבודה בשלבים קטנים. לאחר כל שלב:

- ציין אילו קבצים השתנו.
- הרץ TypeScript, lint ובדיקות רלוונטיות.
- דווח מה נבדק ידנית ב-Mobile, Tablet ו-Desktop.
- אל תכריז שהעבודה הסתיימה עד שכל הנתיבים, התפקידים וקריטריוני הקבלה נבדקו.

